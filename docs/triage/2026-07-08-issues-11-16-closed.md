# Triage verification — issues #11–16 closed (2026-07-08)

All six open issues verified resolved by commits from 2026-07-07/08 and closed.
Verification checked actual code, not commit titles.

| Issue | Title | Resolved by | Verification |
| ----- | ----- | ----------- | ------------ |
| #12 | Runtime dry-run toggle: DB config + drop DRY_RUN env | `c6a6b89` | `dry_run` runtime DB config (`db.py:246`: missing/`"true"` → dry-run, only `"false"` → live), read per scan tick; `rg "DRY_RUN"` finds zero live-code refs in `src/`, `dashboard/`, `run.sh` |
| #13 | Catalog schema: sport_path / final_minutes / min_volume + live | `70a59b6` | `strategies.py`: `Trigger.sport_path` (registry-validated), `Trigger.final_minutes`, `Trigger.min_volume`, `Strategy.live` (default false, strict) |
| #14 | Unified placement path: fires gate real orders | `2041dbf`, `0ffe735`, `462a01e` | `scanner.py` gate places real order iff strategy live-enabled AND dry-run off AND kill switch inactive, else dry-run trade; population tagging per fire; guards restored in `462a01e`; covered by `test_scanner_strategies.py` / `test_dry_run_config.py` |
| #15 | Catalog editor: raw-yaml save + hot reload | `16da7d3` | `GET`/`PUT /api/strategies/raw` (`api.py`): validate → write → next-tick hot reload (ADR-0002), dashboard editor |
| #16 | Dry-run performance viz: population filter + per-strategy P&L | `38a510d` | Live \| Dry-run population filter + per-strategy cumulative counterfactual P&L in dashboard |
| #11 | Parent PRD | all sub-issues above | Runtime dry-run mode, unified single-placement path, dry-run performance viz all landed |

Decisions recorded in `docs/adr/0002-runtime-dry-run-unified-placement.md`.

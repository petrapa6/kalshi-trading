# Requirements Archive: v1.2 Strategy Engine

**Archived:** 2026-05-08
**Status:** ✅ All v1.2 requirements complete (10/10) — 1 documented deviation
**Audit:** [v1.2-MILESTONE-AUDIT.md](v1.2-MILESTONE-AUDIT.md) (`tech_debt` — no blockers, 4 advisory items)

---

## v1 Requirements (Validated — already shipped at v1.0/v1.1)

See `PROJECT.md` Validated section. Tracked there with `✓` markers; sourced from `.planning/codebase/` documents (last mapped on commit `f2c2f78`).

| REQ-ID | Description | Source |
|--------|-------------|--------|
| SCAN-01..04 | Scanner loops, betting filters, settlement duality, WHAT_IF strategies (deprecated by v1.2) | `codebase/ARCHITECTURE.md` |
| CFG-01..02 | Runtime config from SQLite, kill switch | `codebase/STACK.md` |
| DASH-01..02 | Dashboard read-only views + auth gate | `codebase/INTEGRATIONS.md` |
| CLI-01 | React-ink CLI | `codebase/STRUCTURE.md` |
| BT-01..05 | Soccer backtest (v1.0) + local-JSON refactor (v1.1) | `dashboard/app/backtest/`, `src/predictions/backtest.py` |
| INFRA-01..02 | SST deploy + S3 snapshot durability | `codebase/STACK.md`, `sst.config.ts` |

## Active Milestone Requirements (v1.2 — Strategy Engine)

All v1.2 requirements completed and validated by milestone audit (2026-05-08).

### Backtest Engine

- [x] **BT-06**: User backtests use contract-based P&L math: `contracts = floor(stake_cents / price_cents)`, win profit = `contracts × (100 − price_cents)`, loss = `contracts × price_cents`; the `avg_win_yield` input is removed from the backtest UI
  - **Outcome: Validated** — `dashboard/app/backtest/backtest.ts:151,157,159` canonical kernel; node-recomputed worked example confirmed; Phase 01 verification 19/19. `avg_win_yield` removed; `contractPriceCents` slider (50–99¢, default 97) added.

- [x] **BT-07**: The backtest page has a strategy selector dropdown populated from `strategies.yaml`; selecting a strategy pre-populates the parameter sliders with that strategy's first trigger values; sliders remain fully editable so users can explore custom variations after loading a preset
  - **Outcome: Validated** — `dashboard/app/backtest/page.tsx:148,212-215` dropdown wired. D-02 override loaded ALL triggers (not just first); user approved this design at the 02-04 human-verify checkpoint. Sport / min_minute / min_lead are sliders; min_yes_price / max_yes_price are info text per D-11.

### Strategy Definition

- [x] **STR-01**: Named strategies are defined in a `strategies.yaml` file at the repo root (path overridable via `STRATEGIES_PATH` env var), replacing the hardcoded `WHAT_IF_STRATEGIES` list in `scanner.py`; missing file → scanner logs a warning and proceeds with no strategies
  - **Outcome: Validated** — `strategies.yaml` ships 2 active strategies (`conservative_late_lead`, `early_value`). Missing-file behavior verified by `tests/test_strategies.py::test_missing_file_returns_empty` and direct smoke (`load_strategies('/nonexistent.yaml')` → `[]` + WARNING log).

- [x] **STR-02**: Each strategy in `strategies.yaml` supports multi-trigger (OR-of-AND) conditions: a `triggers` list where each entry is a flat dict of AND conditions, and any one entry matching fires the strategy; supported trigger fields: `sport`, `min_lead`, `min_minute`, `min_yes_price`, `max_yes_price`; missing field in a trigger = no constraint on that dimension
  - **Outcome: Validated** — `src/predictions/strategies.py:21-39` OR-of-AND model with Pydantic `extra="forbid"` + `Field(min_length=1)`. `early_value` strategy demonstrates OR-of-AND with 2 triggers.

- [x] **STR-03**: Strategy definitions in `strategies.yaml` drive both the backtest simulator and the live scanner (single source of truth for strategy logic)
  - **Outcome: Validated** — Single `load_strategies()` (only `yaml` import in src/) imported by `scanner.py:44` AND `api.py:35`. No parallel parsers anywhere. The dashboard never reads YAML directly — it fetches `/api/strategies`.

- [x] **STR-04**: The `stretch_opportunities` DB table is dropped and `WHAT_IF_STRATEGIES` is removed from `scanner.py`; the existing `GET /api/sport-stats` endpoint is updated to derive game counts from the `opportunities` table instead
  - **Outcome: Adjusted (deviation)** — Per PROJECT.md decision STR-04 + Plan 03-02 D-03, the table was **renamed**, not dropped (`stretch_opportunities` → `stretch_opportunities_archived`). Production had 6+ months of stretch data; rename is idempotent + rollback-safe vs. data-loss DROP. `WHAT_IF_STRATEGIES` and `_evaluate_what_if_strategies` removed. `/api/sport-stats` rerouted to `opportunities` per D-19. Functional intent (table no longer used downstream) fully met.

### Dry-Run Trading

- [x] **DRY-01**: The live scanner evaluates all enabled strategies from `strategies.yaml` each scan loop; when a strategy's trigger conditions are met for a market, the scanner fires a dry-run trade: no Kalshi API call is made, but the live `yes_ask` price from the market_prices cache (fetched via WebSocket/REST) is recorded as the entry price, hardcoded `dry_run=True` regardless of the process-level `DRY_RUN` env var
  - **Outcome: Validated** — `src/predictions/scanner.py:425-474` `place_strategy_trade` constructs `Trade(... status="dry_run", dry_run=True, strategy_name=strategy_name, yes_price=opp["yes_ask"])` with literal hardcoded values. `evaluate_strategies` invoked per tick at `scanner.py:981`. Regression test (`tests/test_scanner_strategies.py:79`) sets `DRY_RUN=false` and still asserts `dry_run=True` on the resulting Trade row — env-independence is structurally enforced.

- [x] **DRY-02**: Dry-run strategy trades are stored in the `trades` table with a new `strategy_name` column; the recorded `yes_price` (live Kalshi yes_ask at signal time) is used to compute contract-based P&L on settlement: `contracts = floor(bet_amount / yes_price)`, win = `contracts × (100 − yes_price)`, loss = `−contracts × yes_price`; settlement reconciliation runs for these trades (via WebSocket primary + REST fallback)
  - **Outcome: Validated** — `Trade.strategy_name` column added via idempotent `ALTER TABLE` (Plan 03-02 D-01). Settlement composite filter `or_(dry_run==False, and_(dry_run==True, strategy_name.isnot(None)))` is byte-identical between `check_settlements` (REST) at `scanner.py:289-292` and `on_lifecycle` (WS) at `:352-355` (D-16). Both write `pnl_cents`, `status`, and `settled_at` (CR-01 fix in commits 230c2e3 + 7415f39 + b6194c8 during Phase 04 verification).

### Analytics Dashboard

- [x] **DASH-03**: A new dashboard page shows per-strategy dry-run performance: strategy selector, summary stat cards (total trades, wins, losses, win rate, realized P&L), cumulative P&L line chart (x = `placed_at`, y = running P&L sum), and a trade log table (date, ticker, price, contracts, P&L, status); page is behind the `checkAuth` gate
  - **Outcome: Validated** — `dashboard/app/analytics/page.tsx:13,132-137` `checkAuth()` gates render; `:242-267` renders 5 stat cards; recharts P&L line chart at `:269-316`; trade log table renders all required fields. Backend endpoints `GET /api/strategy-analytics` + `GET /api/strategies-summary` (api.py:421-588) behind `Depends(_check_token)`. Phase 04 verification 8/8 must-haves passed after CR-01 closure.
  - **Note (D-09 vs DASH-03 axis):** REQUIREMENTS specified `placed_at` as the chart x-axis; D-09 implemented `settled_at` since the chart shows realized P&L (only settled trades contribute). User approved at Phase 04 verification — `settled_at` is semantically correct for a realized-P&L curve.

- [x] **DASH-04**: The analytics page auto-refreshes every 5 minutes to show new dry-run activity without a manual reload
  - **Outcome: Validated** — `dashboard/app/analytics/page.tsx:192` `setInterval(fetchAll, 5 * 60 * 1000)` with cleanup on unmount. `lastUpdated` state ticks forward at each successful fetch. Runtime confirmation accepted via source-confirmation UAT (88/88 unit tests + clean code path).

## Future Requirements (deferred)

| REQ-ID | Description | Why deferred |
|--------|-------------|--------------|
| `lead_pct` trigger field | % of runtime config `min_lead` (e.g. `lead_pct: 75`) | Flat values sufficient for v1.2 |
| `max_countdown_secs`, `series_ticker` trigger fields | Additional trigger dimensions | Core fields sufficient for v1.2 |
| Per-strategy `bet_percent` override | Override the global runtime config | Sizing stays in SQLite config for now |
| Hot-reload of `strategies.yaml` | Reload without container restart | Open dry-run trades under changed strategies create mismatched state; design needed |
| Strategy editor in the dashboard UI | File-driven config is the v1.2 design | Consider for v1.3+; if added, must include max-file-size check before `safe_load` (per 02-RESEARCH security domain — alias amplification mitigation) |
| Phase 999.1 — Analytics back/forward popstate sync | WR-04 advisory; back button does not re-sync `selected` state | Tracked in ROADMAP.md backlog |

## Out of Scope

| Feature | Reason |
|---------|--------|
| Real-money trading | v1.2 is dry-run only; `trading_paused` kill switch remains in place |
| Kalshi demo/sandbox environment integration | Local simulation sufficient for the 2-week observation goal |
| CLI commands for strategy management | File-driven config + dashboard analytics are sufficient |
| New season JSONs / backtest page UI redesign beyond BT-06 | Out of scope for the strategy engine milestone |

## Traceability (Final)

| REQ-ID | Phase | Final Status |
|--------|-------|--------------|
| BT-06 | Phase 1 | Validated |
| BT-07 | Phase 2 | Validated |
| STR-01 | Phase 2 | Validated |
| STR-02 | Phase 2 | Validated |
| STR-03 | Phase 2 | Validated |
| STR-04 | Phase 3 | Adjusted (RENAME, not DROP) |
| DRY-01 | Phase 3 | Validated |
| DRY-02 | Phase 3 | Validated |
| DASH-03 | Phase 4 | Validated (with D-09 axis adjustment: `settled_at` for realized-P&L semantics) |
| DASH-04 | Phase 4 | Validated |

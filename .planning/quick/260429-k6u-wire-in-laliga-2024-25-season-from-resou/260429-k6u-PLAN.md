---
id: 260429-k6u
type: quick
status: planned
description: Wire in LaLiga 2024/25 season JSON into the backtest seasons catalog
---

# Quick Task 260429-k6u

**Goal:** Surface `resources/laliga_2024_25_season.json` in the backtest dropdown.

## Context

- File `resources/laliga_2024_25_season.json` already exists (untracked, 137 KB, EPL-shape JSON).
- `dashboard/app/backtest/seasons.ts` uses a hand-maintained `IMPORTS` array (per its own TODO comment) — adding a new season requires both an `import` statement and an entry in that list.
- The current `LEAGUE_NAMES` map uses `pd` for La Liga, but the `fetch-football-season` skill emits files prefixed `laliga_*`. Add `laliga` to the map; leave `pd` alone (defensive — could still be used elsewhere) … actually drop it: no producer in this repo emits `pd_*` files, so it's dead code.

## Files to change

- `dashboard/app/backtest/seasons.ts` — add LaLiga import; map `laliga` → `"La Liga"`; drop the dead `pd` entry; add JSON to `IMPORTS`.

## Tasks

### Task 1 — Wire the season

- Add `import laliga_2024_25 from "../../../resources/laliga_2024_25_season.json";` near the existing EPL import.
- Replace `pd: "La Liga"` with `laliga: "La Liga"` in `LEAGUE_NAMES`.
- Append `{ filename: "laliga_2024_25_season.json", data: laliga_2024_25 as SeasonFile }` to `IMPORTS`.

### Task 2 — Track the JSON

- `git add resources/laliga_2024_25_season.json`. Same commit as the seasons wiring (the import would be broken without the file).

### Task 3 — Verify

- `pnpm fmt:check && pnpm lint && pnpm build` green.
- LaLiga shows up as a selectable option in the dropdown.

## Done when

- LaLiga 2024/25 is selectable in the backtest UI alongside EPL 2024/25.
- One atomic commit (resource + wiring), no UI behaviour regressions.

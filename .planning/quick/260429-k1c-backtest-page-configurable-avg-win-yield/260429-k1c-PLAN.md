---
id: 260429-k1c
type: quick
status: planned
description: Backtest page — replace hard-coded WIN_YIELD with configurable Avg win yield input
---

# Quick Task 260429-k1c

**Goal:** Make the win-yield rate (currently fixed at `0.03`) a user-controlled strategy parameter, defaulting to `0.03`.

## Files to change

- `dashboard/app/backtest/backtest.ts` — add `avg_win_yield` to `BacktestParams`; rename exported `WIN_YIELD` → `DEFAULT_WIN_YIELD` (keep as the default constant); read the param inside `runBacktest`.
- `dashboard/app/backtest/page.tsx` — add `avgWinYield` state (default `DEFAULT_WIN_YIELD`), number input, pass it through, update helper text to show the active rate.

## Tasks

### Task 1 — Engine

- Add `avg_win_yield: number` to `BacktestParams`.
- Rename `WIN_YIELD` → `DEFAULT_WIN_YIELD` (keep value `0.03`, keep export).
- In `runBacktest`, replace the constant reference with `avg_win_yield` from params.

### Task 2 — Page

- Add `avgWinYield` state, default to `DEFAULT_WIN_YIELD`.
- Number input next to bet size: min `0.001`, step `0.001`, max e.g. `1`. Stored as a fraction (0.03 = 3%).
- Pass `avg_win_yield: avgWinYield` to `runBacktest`.
- Update helper line: "Win yield: {avgWinYield} EUR per 1 EUR staked. A losing bet loses the full stake."

### Task 3 — Verify

- `pnpm fmt:check && pnpm lint && pnpm build` green.
- Manual sanity: default value 0.03 reproduces the previous result exactly (1000 → 1000.6 on first win at 2% bet).

## Done when

- Engine no longer reads a hard-coded yield; the input controls it.
- Default behaviour is unchanged (regression-safe).
- Lint/format/build green; two atomic commits.

---
phase: 03-scanner-integration
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - src/predictions/api.py
  - dashboard/app/page.tsx
autonomous: true
requirements:
  - STR-04
tags:
  - api
  - dashboard
  - cleanup
  - phase-3
must_haves:
  truths:
    - "D-19: GET /api/sport-stats re-sourced from `opportunities` table — `SELECT series_ticker, COUNT(DISTINCT event_ticker) FROM opportunities WHERE series_ticker IS NOT NULL GROUP BY series_ticker`. Bearer-auth via Depends(_check_token) preserved (PATTERNS.md S5). Response shape unchanged (still `{stats: {LABEL: {played, wins, pnl}}}`); the meaning of `played` shifts from near-miss-rows to distinct-events-scanned. ROADMAP success criterion #1 final piece."
    - "D-21: GET /api/stretch-stats endpoint + handler get_stretch_stats DELETED entirely (api.py:826-878). DELETE /api/stretch endpoint + handler clear_stretch_opportunities DELETED entirely (api.py:881-895). The internal helper _compute_stretch_stats becomes orphaned and is DELETED. The Pydantic models StretchStatsResponse + StrategySetStats become orphaned and are DELETED. The `from predictions.scanner import WHAT_IF_STRATEGIES` import inside get_stretch_stats is gone with the function. The `StretchOpportunity` import in api.py:21 is removed."
    - "D-21 dashboard: dashboard/app/page.tsx — the `\"strategy\"` mainTab id + label removed from the tab list (line ~2399), removed from the mainTab union type (line 2202-2210), removed from the conditional render block (lines 2618-2691). The `stretchStats` useState declaration (line 2192) + `setStretchStats` setter + the `fetch(\\${API}/api/stretch-stats)` call inside the slow-poll Promise.all (line 2261) + `if (stretchRes.ok) setStretchStats(...)` (line 2266) all DELETED. The `interface StretchStats` (lines 145-157) + `interface StrategySetStats` (line 131) DELETED. After deletion `pnpm lint && pnpm fmt:check && pnpm build` passes clean."
    - "D-22 (api consumer follow-up): tests/test_sport_stats.py was migrated in Plan 03-01 to seed Opportunity rows. After D-19 ships, the xfail marker on test_mlb_and_mlbst_are_kept_distinct is REMOVED and the test passes against the new query."
    - "Bearer-auth invariant preserved: /api/sport-stats keeps `dependencies=[Depends(_check_token)]`. No new endpoints added. The two deleted endpoints had _check_token; their removal does NOT introduce an unauthenticated surface."
    - "Behavior change documented: under D-19, `played` count means `COUNT(DISTINCT event_ticker)` from opportunities (i.e. distinct games scanned per series) rather than rows in stretch_opportunities (near-misses). The dashboard's Sports tab consumes this number as a 'did anything happen?' indicator — semantic shift is invisible to the UI rendering. Cross-checked in PATTERNS.md."
    - "Phase 4 placeholder: the deleted dashboard 'Strategy' tab is replaced by a NEW per-strategy analytics page in Phase 4 (DASH-03). Phase 3 leaves the gap intentionally — no v1 placeholder, no temporary read from stretch_opportunities_archived."
  artifacts:
    - path: "src/predictions/api.py"
      provides: "GET /api/sport-stats sourced from opportunities, with COUNT(DISTINCT event_ticker)"
      contains: "FROM opportunities"
    - path: "src/predictions/api.py"
      provides: "Endpoint deletions complete: get_stretch_stats, clear_stretch_opportunities, _compute_stretch_stats, StretchStatsResponse, StrategySetStats — all gone"
      exports: ["app", "get_total_sport_stats"]
    - path: "dashboard/app/page.tsx"
      provides: "Strategy tab + stretchStats state + StretchStats interface + StrategySetStats interface — all removed"
      contains: "interface SportStats"
  key_links:
    - from: "src/predictions/api.py::get_total_sport_stats"
      to: "predictions.db.Opportunity (table opportunities)"
      via: "raw SQL via text() — COUNT(DISTINCT event_ticker)"
      pattern: "FROM opportunities"
    - from: "dashboard/app/page.tsx::slow-poll Promise.all"
      to: "GET /api/sport-stats only (no /api/stretch-stats)"
      via: "fetch(`${API}/api/sport-stats`) preserved; /api/stretch-stats removed"
      pattern: "/api/stretch-stats"
---

<objective>
Wave 3 cleanup. Land all `api.py` and `dashboard/app/page.tsx` changes that
finalize the WHAT_IF system decommission and re-source `/api/sport-stats`.
This plan unblocks the final ROADMAP success criterion #1 (sport-stats from
opportunities) and finalizes D-19 + D-21 + the api.py side of D-20/D-22.

Decisions implemented:
- D-19: /api/sport-stats query swap to opportunities table
- D-21: delete /api/stretch-stats + DELETE /api/stretch + dashboard "Strategy" tab + stretchStats state + StretchStats/StrategySetStats interfaces
- D-20 (api.py portion): remove StretchOpportunity import + delete orphaned response models + delete orphaned _compute_stretch_stats helper
- D-22 (api consumer): test_sport_stats.py xfail marker dropped (test now passes against the new D-19 query)

Purpose: All ROADMAP Phase 3 success criteria are now demonstrably true. After
this plan ships, `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pytest tests/`
all exit 0; `(cd dashboard && pnpm lint && pnpm fmt:check && pnpm build)` exits 0.

Output: api.py + dashboard/app/page.tsx committed. Test suite full green.
Manual UAT: dashboard renders with no "Strategy" tab and the Sports tab still
shows sane numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scanner-integration/03-CONTEXT.md
@.planning/phases/03-scanner-integration/03-RESEARCH.md
@.planning/phases/03-scanner-integration/03-PATTERNS.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONVENTIONS.md
@src/predictions/api.py
@dashboard/app/page.tsx
@tests/test_sport_stats.py

<interfaces>
Existing api.py contracts.

From src/predictions/api.py:17-36 (current imports — REMOVE StretchOpportunity):
```
from predictions.db import (
    BalanceSnapshot,
    Opportunity,
    Scan,
    StretchOpportunity,   # REMOVE
    Trade,
    get_all_config,
    get_session,
    init_db,
    set_config,
)
```

From src/predictions/api.py:128-147 (Pydantic models — REMOVE StrategySetStats + StretchStatsResponse):
```
class StrategySetStats(BaseModel):
    label: str
    total: int
    wins: int; losses: int; open: int
    win_rate: float
    hypothetical_pnl_cents: int
    by_reason: dict[str, dict]

class StretchStatsResponse(BaseModel):
    total: int
    wins: int; losses: int; open: int
    win_rate: float
    hypothetical_pnl_cents: int
    by_reason: dict[str, dict]
    strategies: dict[str, StrategySetStats]
```

From src/predictions/api.py:484-554 (current sport-stats — replace SQL on lines 491-498):
```
@app.get("/api/sport-stats", dependencies=[Depends(_check_token)])
def get_total_sport_stats():
    session = get_session()
    from sqlalchemy import text
    seen_matches = session.execute(
        text("SELECT series_ticker, COUNT(DISTINCT event_ticker) FROM stretch_opportunities GROUP BY series_ticker")
    ).fetchall()
    real_trades = (...)   # downstream aggregation logic UNCHANGED
    session.close()
    stats = {}
    # ... ticker_prefix_map + label aggregation (unchanged)
    return {"stats": stats}
```

From src/predictions/api.py:792-823 (orphan helper after D-21):
```
def _compute_stretch_stats(stretches: list) -> dict:
    """Compute stats for a list of stretch opportunities."""
    # ... DELETE entirely after D-21 — only caller is get_stretch_stats
```

From src/predictions/api.py:826-895 (DELETE both endpoints):
```
@app.get("/api/stretch-stats", response_model=StretchStatsResponse, dependencies=[Depends(_check_token)])
def get_stretch_stats():
    from predictions.scanner import WHAT_IF_STRATEGIES   # <-- breaks after Plan 03-03
    # ... full handler body — DELETE

@app.delete("/api/stretch", dependencies=[Depends(_check_token)])
def clear_stretch_opportunities():
    # ... full handler body — DELETE
```

From dashboard/app/page.tsx:131-157 (StrategySetStats + StretchStats — DELETE both):
```
interface StrategySetStats {
  label: string;
  // ...
}

interface StretchStats {
  total: number;
  // ...
  strategies: Record<string, StrategySetStats>;
}
```

From dashboard/app/page.tsx:2192 (state declaration — DELETE):
```
const [stretchStats, setStretchStats] = useState<StretchStats | null>(null);
```

From dashboard/app/page.tsx:2202-2210 (mainTab union — REMOVE "strategy"):
```
const [mainTab, setMainTab] = useState<
  | "overview"
  | "charts"
  | "sports"
  | "live_games"
  | "strategy"   // REMOVE
  | "config"
  | "trades"
>("overview");
```

From dashboard/app/page.tsx:2257-2272 (slow-poll fetch — REMOVE stretchStats slot):
```
const [tradesRes, stretchRes, configRes, ssRes] = await Promise.all([
  fetch(`${API}/api/histogram-trades?limit=10000`),
  fetch(`${API}/api/stretch-stats`),     // REMOVE
  fetch(`${API}/api/config`),
  fetch(`${API}/api/sport-stats`),
]);
if (tradesRes.ok) setAllTrades((await tradesRes.json()).trades ?? []);
if (stretchRes.ok) setStretchStats(await stretchRes.json());   // REMOVE
if (configRes.ok) setConfig(await configRes.json());
if (ssRes.ok) setSportStats((await ssRes.json()).stats);
```

From dashboard/app/page.tsx:2392-2415 (tab list — REMOVE "strategy" entry):
```
{[
  { id: "overview", label: "Overview" },
  { id: "charts", label: "Charts" },
  { id: "sports", label: "Sports" },
  { id: "live_games", label: "Live Games" },
  { id: "strategy", label: "Strategy" },   // REMOVE
  { id: "config", label: "Config" },
  { id: "trades", label: "Recent Trades" },
].map(...)}
```

From dashboard/app/page.tsx:2618-2691 (the "Strategy" mainTab body — DELETE entire block).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate /api/sport-stats to opportunities table (D-19)</name>
  <files>src/predictions/api.py, tests/test_sport_stats.py</files>
  <read_first>
    - .planning/phases/03-scanner-integration/03-CONTEXT.md D-19 (lines 330–352)
    - .planning/phases/03-scanner-integration/03-RESEARCH.md "Code Examples Example 5" (lines 832–860) — exact SQL recipe
    - .planning/phases/03-scanner-integration/03-PATTERNS.md "/api/sport-stats (MOD per D-19)" — confirms aggregation logic unchanged below the query
    - src/predictions/api.py:484-554 — current handler
    - tests/test_sport_stats.py — Plan 03-01 already migrated to Opportunity seed; xfail still present
  </read_first>
  <action>
TWO edits.

Edit 1 — src/predictions/api.py:484-498. Replace the SQL query inside get_total_sport_stats:

Replace:

```
    session = get_session()

    # Get all unique matches seen from StretchOpportunities (which tracks all markets
    # reaching final periods). Group by series_ticker and count unique event_tickers.
    from sqlalchemy import text

    seen_matches = session.execute(
        text(
            "SELECT series_ticker, COUNT(DISTINCT event_ticker) "
            "FROM stretch_opportunities GROUP BY series_ticker"
        )
    ).fetchall()
```

With:

```
    session = get_session()

    # Phase 3 D-19: source from opportunities table (replacing
    # stretch_opportunities). Behavior change: `played` is now distinct
    # event_tickers seen by the scanner per series — i.e. games scanned,
    # not near-miss rows. Same response shape; aggregation logic below
    # is unchanged.
    from sqlalchemy import text

    seen_matches = session.execute(
        text(
            "SELECT series_ticker, COUNT(DISTINCT event_ticker) "
            "FROM opportunities "
            "WHERE series_ticker IS NOT NULL "
            "GROUP BY series_ticker"
        )
    ).fetchall()
```

The `WHERE series_ticker IS NOT NULL` is defensive — Opportunity.series_ticker is populated by scan_kalshi_with_espn but the column is nullable.

Aggregation logic in lines 511-554 (ticker_prefix_map, _add helper, real_trades query, label aggregation, return shape) is UNCHANGED.

Edit 2 — tests/test_sport_stats.py. Drop the xfail marker on `test_mlb_and_mlbst_are_kept_distinct` that Plan 03-01 added. The test already seeds Opportunity rows; once the query is swapped above, the assertions pass.

Format with oxfmt 4-space; ruff-clean; ty-clean.
  </action>
  <verify>
    <automated>cd /home/pavel/playground/kalshi-trading && grep -q "FROM opportunities" src/predictions/api.py && grep -q "WHERE series_ticker IS NOT NULL" src/predictions/api.py && (! grep -q "@pytest.mark.xfail" tests/test_sport_stats.py) && uv run pytest tests/test_sport_stats.py -x</automated>
  </verify>
  <done>
- `grep -q "FROM opportunities" src/predictions/api.py` exits 0.
- `! grep -q "FROM stretch_opportunities GROUP BY series_ticker" src/predictions/api.py` exits 0 (old query gone).
- `grep -q "WHERE series_ticker IS NOT NULL" src/predictions/api.py` exits 0.
- `! grep -q "@pytest.mark.xfail" tests/test_sport_stats.py` exits 0.
- `uv run pytest tests/test_sport_stats.py -x` exits 0 (one test passes).
- `uv run ruff check src/predictions/api.py` exits 0 (note: ty check still red on api.py until Task 2 removes StretchOpportunity-dependent endpoints).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete stretch endpoints, response models, and StretchOpportunity import (D-20, D-21)</name>
  <files>src/predictions/api.py</files>
  <read_first>
    - .planning/phases/03-scanner-integration/03-CONTEXT.md D-21 (lines 374–399), D-20 (lines 354–372)
    - .planning/phases/03-scanner-integration/03-PATTERNS.md "/api/stretch-stats and DELETE /api/stretch (DELETE per D-21)" + "Bearer-auth on FastAPI endpoints" (S5)
    - src/predictions/api.py:17-36 (imports), :128-147 (Pydantic models), :792-895 (helpers + endpoints to delete)
  </read_first>
  <action>
FOUR coordinated deletions in src/predictions/api.py.

Edit 1 — Remove `StretchOpportunity` from the imports block (api.py:17-27). Replace:

```
from predictions.db import (
    BalanceSnapshot,
    Opportunity,
    Scan,
    StretchOpportunity,
    Trade,
    get_all_config,
    get_session,
    init_db,
    set_config,
)
```

With:

```
from predictions.db import (
    BalanceSnapshot,
    Opportunity,
    Scan,
    Trade,
    get_all_config,
    get_session,
    init_db,
    set_config,
)
```

Edit 2 — Delete `class StrategySetStats(BaseModel):` (api.py:128-136) and `class StretchStatsResponse(BaseModel):` (api.py:139-147). Both classes become orphaned after the endpoint deletions in Edit 4.

Edit 3 — Delete `_compute_stretch_stats` helper (api.py:792-823). Only caller is get_stretch_stats (deleted in Edit 4).

Edit 4 — Delete the two endpoint handlers + their decorators:
- `@app.get("/api/stretch-stats", ...)` + `def get_stretch_stats():` (api.py:826-878)
- `@app.delete("/api/stretch", ...)` + `def clear_stretch_opportunities():` (api.py:881-895)

Note: get_stretch_stats has `from predictions.scanner import WHAT_IF_STRATEGIES` inside its body (api.py:830). After Plan 03-03 ships, that import would raise ImportError — so deleting the function is the correct cleanup, not a workaround.

Verify orphans by grep:
- `! grep -q "StretchOpportunity" src/predictions/api.py` — should pass (none should remain).
- `! grep -q "WHAT_IF_STRATEGIES" src/predictions/api.py` — should pass.
- `! grep -q "_compute_stretch_stats" src/predictions/api.py` — should pass.
- `! grep -q "StretchStatsResponse" src/predictions/api.py` — should pass.
- `! grep -q "StrategySetStats" src/predictions/api.py` — should pass.
- `! grep -q "/api/stretch" src/predictions/api.py` — should pass (both `/api/stretch-stats` and `/api/stretch` gone; `/api/sport-stats` is unaffected since it doesn't contain `/api/stretch`).

Format with oxfmt 4-space; ruff-clean; ty-clean.
  </action>
  <verify>
    <automated>cd /home/pavel/playground/kalshi-trading && uv run ruff check src/predictions/api.py && uv run ruff format --check src/predictions/api.py && (! grep -q "StretchOpportunity" src/predictions/api.py) && (! grep -q "WHAT_IF_STRATEGIES" src/predictions/api.py) && (! grep -q "_compute_stretch_stats" src/predictions/api.py) && (! grep -q "StretchStatsResponse" src/predictions/api.py) && (! grep -q "StrategySetStats" src/predictions/api.py) && (! grep -q "/api/stretch-stats\\|/api/stretch[^-]" src/predictions/api.py) && uv run ty check && uv run pytest tests/ -x</automated>
  </verify>
  <done>
- All 6 negative greps exit 0.
- `uv run ruff check src/predictions/api.py` exits 0.
- `uv run ruff format --check src/predictions/api.py` exits 0.
- `uv run ty check` exits 0 (full Phase 3 backend now type-clean).
- `uv run pytest tests/ -x` exits 0 (full suite green).
- NOTE: Plan 03-04's plan-level success_criteria require Task 3 (dashboard build) to ALSO complete; this <done> only attests Task 2 is finished. The dashboard tab removal is a separate file (`dashboard/app/page.tsx`) and is verified by Task 3's verify command.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove dashboard "Strategy" tab + stretchStats state + StretchStats interfaces (D-21)</name>
  <files>dashboard/app/page.tsx</files>
  <read_first>
    - .planning/phases/03-scanner-integration/03-CONTEXT.md D-21 (lines 374–399)
    - .planning/phases/03-scanner-integration/03-PATTERNS.md "dashboard/app/page.tsx — 'What If? Strategy Comparison' tab + /api/stretch-stats consumer (DELETE per D-21)" — exact line ranges
    - dashboard/app/page.tsx — full file context for the affected sections (lines 131-157, 2188-2280, 2392-2415, 2618-2691)
  </read_first>
  <action>
SIX deletions in dashboard/app/page.tsx. Each is precise — verify the LSP diagnostics after each edit (CLAUDE.md "After writing or editing code, wait 0.5s for LSP to catch up").

Edit 1 — Delete `interface StrategySetStats` (page.tsx:131-143) and `interface StretchStats` (page.tsx:145-157). Both are orphaned after Edit 5.

Edit 2 — Delete `const [stretchStats, setStretchStats] = useState<StretchStats | null>(null);` (page.tsx:2192). Verify no other reference to `setStretchStats` or `stretchStats` outside the deletions in Edits 5/6.

Edit 3 — Remove `"strategy"` from the mainTab union type (page.tsx:2202-2210). The post-edit type:

```
const [mainTab, setMainTab] = useState<
  | "overview"
  | "charts"
  | "sports"
  | "live_games"
  | "config"
  | "trades"
>("overview");
```

Edit 4 — In the slow-poll Promise.all (page.tsx:2257-2272), remove the stretch-stats fetch. Replace:

```
const [tradesRes, stretchRes, configRes, ssRes] = await Promise.all([
  fetch(`${API}/api/histogram-trades?limit=10000`),
  fetch(`${API}/api/stretch-stats`),
  fetch(`${API}/api/config`),
  fetch(`${API}/api/sport-stats`),
]);
if (tradesRes.ok) setAllTrades((await tradesRes.json()).trades ?? []);
if (stretchRes.ok) setStretchStats(await stretchRes.json());
if (configRes.ok) setConfig(await configRes.json());
if (ssRes.ok) setSportStats((await ssRes.json()).stats);
```

With:

```
const [tradesRes, configRes, ssRes] = await Promise.all([
  fetch(`${API}/api/histogram-trades?limit=10000`),
  fetch(`${API}/api/config`),
  fetch(`${API}/api/sport-stats`),
]);
if (tradesRes.ok) setAllTrades((await tradesRes.json()).trades ?? []);
if (configRes.ok) setConfig(await configRes.json());
if (ssRes.ok) setSportStats((await ssRes.json()).stats);
```

Edit 5 — In the tab list (page.tsx:2392-2415), remove `{ id: "strategy", label: "Strategy" }` from the array. Verify no orphan trailing comma.

Edit 6 — Delete the entire `{mainTab === "strategy" && (...)}` conditional render block (page.tsx:2618-2691). Roughly 73 lines starting from `{mainTab === "strategy" && (` and ending at the closing `)}`. Be precise; this block is bounded by JSX braces, ensure the deletion balances both opening and closing.

Final grep verification (run after all edits):
- `! grep -q "StretchStats\\|StrategySetStats\\|stretchStats\\|setStretchStats" dashboard/app/page.tsx` — must pass.
- `! grep -q "/api/stretch-stats" dashboard/app/page.tsx` — must pass.
- `! grep -q "What If? Strategy Comparison" dashboard/app/page.tsx` — must pass.
- `! grep -q "id: \"strategy\"" dashboard/app/page.tsx` — must pass.
- `! grep -q "mainTab === \"strategy\"" dashboard/app/page.tsx` — must pass.

Build verification per CLAUDE.md: `(cd dashboard && pnpm lint && pnpm fmt:check && pnpm build)` exits 0.
  </action>
  <verify>
    <automated>cd /home/pavel/playground/kalshi-trading && (! grep -q "StretchStats\\|StrategySetStats\\|stretchStats\\|setStretchStats" dashboard/app/page.tsx) && (! grep -q "/api/stretch-stats" dashboard/app/page.tsx) && (! grep -q "What If? Strategy Comparison" dashboard/app/page.tsx) && (! grep -q 'id: "strategy"' dashboard/app/page.tsx) && (cd dashboard && pnpm lint && pnpm fmt:check && pnpm build) 2>&1 | tail -5</automated>
  </verify>
  <done>
- All 5 negative greps exit 0.
- `(cd dashboard && pnpm lint)` exits 0.
- `(cd dashboard && pnpm fmt:check)` exits 0.
- `(cd dashboard && pnpm build)` exits 0.
- LSP diagnostics on dashboard/app/page.tsx report zero errors (manual confirmation in SUMMARY).
- Manual UAT note (SUMMARY): load `pnpm dev:dashboard`, confirm tabs are: Overview, Charts, Sports, Live Games, Config, Recent Trades. Confirm Sports tab still renders sane numbers (a typical NBA night should be ~5–15 distinct games per series, not 0 or 1000 — per VALIDATION.md Manual-Only row 4).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bearer-auth gate (Depends(_check_token)) ↔ FastAPI endpoints | All Phase 3 endpoint changes preserve auth. /api/sport-stats keeps its existing dependency; the two deleted endpoints had auth and are gone — no auth surface change. |
| Dashboard ↔ /api/sport-stats | Slow-poll cadence (60s) preserved; consumer uses .stats response shape unchanged. |
| Dashboard ↔ /api/stretch-stats | Removed entirely — dashboard no longer fetches the deleted endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06 | Information disclosure | /api/sport-stats semantic shift could mislead a consumer treating the number as "near-miss events" | accept | Documented in PATTERNS.md + this PLAN must_haves. The dashboard treats it as a "did anything happen?" indicator; semantic shift invisible to UI rendering. Manual UAT row in 03-VALIDATION.md verifies sane numbers. |
| T-3-07 | Tampering | SQL injection in the new sport-stats text() query | accept | Static SQL string with no user input. SQLAlchemy text() is fine for static DDL/DML; parameterization not needed. |
| T-3-12 | Denial of service / data integrity | COUNT(DISTINCT event_ticker) over a large opportunities table is slow under analytics polling | accept | opportunities has ix_opportunities_found_at index already; series_ticker is repeated (low cardinality). Production opportunities table is ~50K rows; aggregation is sub-second. Bearer-auth gates polling rate. |
| T-3-13 | Elevation of privilege | Removed DELETE /api/stretch — was the bearer-protected wipe endpoint | accept | Deletion removes capability, not adds it. No new admin path. The data is preserved in `stretch_opportunities_archived` (D-03) — wipe is no longer needed because the table is no longer read by app code. If a future audit-clean is needed, an ad-hoc SQL command can DROP the archive table. |
| T-3-14 | Information disclosure (frontend) | Dashboard removal of stretchStats state + StretchStats interface — could leave dangling references that crash the React render | mitigate | Edits 1-6 are precise and grep-verified. `pnpm build` is the gate — TypeScript will error on any dangling reference. LSP-based diagnostic check after each edit per CLAUDE.md. |
</threat_model>

<verification>
After all tasks complete, run from repo root:

```
uv run ruff check src/predictions/api.py
uv run ruff format --check src/predictions/api.py
uv run ty check
uv run pytest tests/
# Full backend suite green; exit 0

(cd dashboard && pnpm lint && pnpm fmt:check && pnpm build)
# Frontend type-clean and builds; exit 0

# Manual UAT (deferred to phase end):
#   pnpm dev:api  +  pnpm dev:dashboard
#   Load http://localhost:3777 with API_TOKEN
#   Verify tabs: Overview / Charts / Sports / Live Games / Config / Recent Trades (no "Strategy")
#   Click Sports tab — confirm sane numbers per series
#   Curl: API_TOKEN=$X curl -H "Authorization: Bearer $X" http://localhost:8000/api/sport-stats | jq '.stats'
#   Verify: { "MLBST": { played: <=N>, ... }, "MLB": ..., ... }
#   Curl: API_TOKEN=$X curl -H "Authorization: Bearer $X" http://localhost:8000/api/stretch-stats
#   Verify: 404 (endpoint deleted)
```

ROADMAP success criteria — final cross-check:
- #1 (rename + WHAT_IF_STRATEGIES removed + sport-stats from opportunities): all 3 sub-conditions hold.
- #2 (strategy fire writes Trade with dry_run=True + strategy_name + yes_price = live yes_ask, no Kalshi REST): Plan 03-03 already shipped; Plan 03-04 doesn't touch.
- #3 (trading_paused blocks dry-run strategy trades): Plan 03-03.
- #4 (settlement on dry_run+strategy_name IS NOT NULL via WS + REST): Plan 03-03.
- #5 (connect_args timeout=5): Plan 03-02.

All 5 success criteria true after this plan ships.
</verification>

<success_criteria>
- src/predictions/api.py contains: `FROM opportunities` + `WHERE series_ticker IS NOT NULL` in get_total_sport_stats.
- src/predictions/api.py does NOT contain: StretchOpportunity, WHAT_IF_STRATEGIES, _compute_stretch_stats, StretchStatsResponse, StrategySetStats, /api/stretch-stats, /api/stretch (the standalone DELETE endpoint).
- dashboard/app/page.tsx does NOT contain: StretchStats, StrategySetStats, stretchStats, setStretchStats, /api/stretch-stats, "What If? Strategy Comparison", id: "strategy", mainTab === "strategy".
- tests/test_sport_stats.py xfail marker dropped; test passes.
- uv run ruff check + ruff format --check + ty check + pytest tests/ all exit 0.
- (cd dashboard && pnpm lint && pnpm fmt:check && pnpm build) all exit 0.
- ROADMAP success criterion #1 fully satisfied.
- 03-VALIDATION.md manual rows 1, 3, 4 retain documented procedures (executed during phase UAT).
</success_criteria>

<output>
After completion, create `.planning/phases/03-scanner-integration/03-04-SUMMARY.md` documenting:
- All decisions implemented (D-19, D-20 api portion, D-21, D-22 api consumer)
- The exact line ranges deleted in api.py and page.tsx (post-implementation actuals)
- The /api/sport-stats semantic shift (near-miss rows → distinct events scanned) flagged for ops awareness
- Confirmation that no new endpoints were added; bearer-auth surface unchanged
- LSP diagnostic clean status on the modified files
- Manual UAT capture: dashboard tab list screenshot/text + Sports tab numbers + curl response from /api/sport-stats
- Final ROADMAP success criteria cross-check (all 5 true)
- Phase-level smoke: full pytest + full ruff/ty + full pnpm pipeline all green
</output>

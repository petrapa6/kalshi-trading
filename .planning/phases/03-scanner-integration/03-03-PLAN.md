---
phase: 03-scanner-integration
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/predictions/scanner.py
autonomous: true
requirements:
  - STR-04
  - DRY-01
  - DRY-02
tags:
  - scanner
  - strategy-eval
  - settlement
  - phase-3
must_haves:
  truths:
    - "D-04: New `evaluate_strategies(session, espn_final_period, max_bet_cents)` is called inside `kalshi_scan_loop` after `scan_kalshi_with_espn` returns and before `check_settlements`. Same iteration cadence (~5s); reuses session, market_prices cache, ESPN cache. NO new asyncio loop."
    - "D-05: `load_strategies()` is invoked ONCE per scan loop iteration. On parse error, log + skip evaluation this tick. NO raise. Mirrors existing get_config_int re-read cadence (no caching, no hot-reload bookkeeping)."
    - "D-06: Iteration is markets × strategies × triggers; trigger_matches evaluates AND on (sport, min_minute, min_lead, min_yes_price, max_yes_price); missing field = no constraint."
    - "D-07: trigger.sport is a sport-FAMILY literal (`football`, `baseball`, `basketball`, `tennis`, `american_football`, `hockey` — UK terminology, NEVER `soccer`). Phase 2 D-02 OVERRIDE governs (02-CONTEXT.md lines 482–496)."
    - "D-08: SPORT_FAMILY_TO_PATHS module-level dict in scanner.py next to MIN_SCORE_LEAD; SPORT_PATH_TO_FAMILY reverse map computed once at import. Only paths actually present in espn.KALSHI_TO_ESPN are enumerated."
    - "D-09: SPORT_PERIOD_LENGTH_SECS + CLOCKLESS_SPORT_PATHS + COUNT_UP_SPORT_PATHS constants; elapsed_minutes(sport_path, clock_seconds, period) returns int | None. Soccer count-up: clock//60. Count-down: (period-1)*period_secs + (period_secs - clock). Clockless: None (caller skips trigger)."
    - "D-10: Per-strategy dedupe by (strategy_name, ticker) via DB query before fire — regardless of row status. A strategy fires at most once per market lifetime."
    - "D-11: Multiple distinct strategies CAN fire on the same ticker — each gets its own Trade row."
    - "D-12: First-trigger-wins per (strategy, market) tick. Within one strategy's triggers list, first matching AND-set fires; remaining triggers skipped via `break`."
    - "D-13: New `place_strategy_trade(session, opp, strategy_name, max_cost_cents)` HARDCODES dry_run=True, status='dry_run', strategy_name=name, yes_price=opp['yes_ask'], count=max_cost_cents//yes_price, cost_cents = count*yes_price, potential_profit_cents = count*(100-yes_price), order_id=None, fee_cents=None. NEVER calls client.create_order(). Does NOT reuse place_bet's dry_run branch — distinct intent."
    - "D-14: max_bet_cents = bet_percent × current_balance, computed ONCE per scan iteration in kalshi_scan_loop, reused for both place_bet and evaluate_strategies. IMPLEMENTED LITERALLY: scan_kalshi_with_espn is REFACTORED to accept max_bet_cents as a positional argument (lifted out of its body); kalshi_scan_loop computes the value once before calling scan_kalshi_with_espn AND passes the same value to evaluate_strategies. NO second client.get_balance() in evaluate_strategies wiring."
    - "Shared-interface flag (CONVENTIONS.md \"flag the blast radius\"): scan_kalshi_with_espn signature changes from (client, espn_final, min_yes_price, bet_percent, dry_run, espn_final_period=None) to (client, espn_final, min_yes_price, max_bet_cents, dry_run, espn_final_period=None) — bet_percent removed (no longer needed inside) and max_bet_cents added. The only caller is kalshi_scan_loop (verified by grep). Tests do not call this function directly."
    - "D-23 SUPERSEDES D-15: `trading_paused == 'true'` check is at the LOOP level — top of evaluate_strategies, early return. Mirrors scanner.py:523-525 pattern. Does NOT iterate strategies × markets when paused. D-15's prose is governed by D-23."
    - "D-16: check_settlements uses combined filter `Trade.status.in_(('placed','filled','dry_run')) AND or_(Trade.dry_run == False, and_(Trade.dry_run == True, Trade.strategy_name.isnot(None)))`. EXCLUDES legacy process-level dry-runs (dry_run=True AND strategy_name IS NULL)."
    - "D-17: on_lifecycle WS handler uses the SAME combined filter, AND-ed with Trade.ticker == ticker. WS path and REST path settle the SAME row set — symmetry is a correctness requirement (per ARCHITECTURE.md Settlement Duality)."
    - "D-18: Strategy dry-run P&L: WIN pnl_cents = count*(100-yes_price), LOSS pnl_cents = -count*yes_price. NO fee (no real order). Status flow: dry_run → settled_win | settled_loss. Same enum values as real trades."
    - "D-20 (scanner.py portion): WHAT_IF_STRATEGIES dict, _evaluate_what_if_strategies function, stretch_opps accumulator + meets_stretch_lead branches inside scan_kalshi_with_espn, the `if stretch_opps:` flush block, the _evaluate_what_if_strategies call site, the StretchOpportunity import, the `from predictions.espn import _espn_to_kalshi_codes` import (if present), and the on_lifecycle stretch_opportunities update block — all REMOVED. Type-clean (uv run ty check) after deletion."
    - "D-22: check_stretch_settlements function (scanner.py:710-747) deleted along with its sole call site at scanner.py:998. The function has no other callers and would become dead code after D-20."
    - "place_bet's existing dry_run=True branch (process-level DRY_RUN env) MUST remain functional and unchanged — two dry-run modes coexist intentionally per D-13 / 03-CONTEXT.md specifics."
    - "load_strategies is the only YAML reader; no parallel parser added in scanner.py."
    - "extract_cents/extract_volume are the only Kalshi price extractors; place_strategy_trade reads yes_ask from market_prices cache (already extracted)."
    - "Integer cents end-to-end: yes_price, cost_cents, potential_profit_cents, pnl_cents are integers throughout — NO float dollars in P&L math."
    - "Pitfall #7 mitigation (RESEARCH 03-RESEARCH.md): kalshi_scan_loop's market_prices seed at scanner.py:954-957 is extended to include `title` and `event_ticker` so evaluate_strategies can call match_kalshi_to_espn(ticker, title, [game]) for soccer fuzzy matching."
    - "on_lifecycle is extracted from its closure inside run_scanner to a module-level async function so tests/test_strategy_settlement.py can exercise it (planner judgment per PATTERNS.md note). The closure captured `client` for record_balance — extraction passes client as a parameter."
  artifacts:
    - path: "src/predictions/scanner.py"
      provides: "SPORT_FAMILY_TO_PATHS, SPORT_PATH_TO_FAMILY, SPORT_PERIOD_LENGTH_SECS, CLOCKLESS_SPORT_PATHS, COUNT_UP_SPORT_PATHS module-level constants"
      contains: "SPORT_FAMILY_TO_PATHS"
    - path: "src/predictions/scanner.py"
      provides: "elapsed_minutes(sport_path, clock_seconds, period) -> int | None"
      contains: "def elapsed_minutes"
    - path: "src/predictions/scanner.py"
      provides: "evaluate_strategies + trigger_matches + place_strategy_trade"
      contains: "async def evaluate_strategies\\|def trigger_matches\\|def place_strategy_trade"
    - path: "src/predictions/scanner.py"
      provides: "check_settlements + on_lifecycle (extracted, module-level) using combined or_/and_ filter"
      contains: "or_(\\|and_(Trade.dry_run == True, Trade.strategy_name.isnot(None))"
  key_links:
    - from: "src/predictions/scanner.py::kalshi_scan_loop"
      to: "scanner.evaluate_strategies"
      via: "await evaluate_strategies(session, current_espn_fp, max_bet_cents) inserted between scan_kalshi_with_espn and check_settlements"
      pattern: "await evaluate_strategies"
    - from: "src/predictions/scanner.py::evaluate_strategies"
      to: "predictions.strategies.load_strategies"
      via: "import + per-tick call (single boundary, D-05)"
      pattern: "from predictions.strategies import load_strategies"
    - from: "src/predictions/scanner.py::evaluate_strategies"
      to: "scanner.place_strategy_trade"
      via: "called once per (strategy, market) match (D-12 first-trigger-wins)"
      pattern: "place_strategy_trade("
    - from: "src/predictions/scanner.py::place_strategy_trade"
      to: "predictions.db.Trade.strategy_name"
      via: "Trade row write with strategy_name=name (D-13)"
      pattern: "strategy_name=strategy_name"
    - from: "src/predictions/scanner.py::check_settlements + on_lifecycle"
      to: "Trade.dry_run == True AND Trade.strategy_name.isnot(None)"
      via: "combined or_/and_ filter (D-16, D-17)"
      pattern: "Trade.strategy_name.isnot(None)"
---

<objective>
Wave 2 scanner integration. Land all `scanner.py` changes implementing live
strategy evaluation, dry-run trade write, settlement reconciliation, and
WHAT_IF system removal in a single coordinated diff.

Decisions implemented (all locked in 03-CONTEXT.md, with D-23 superseding D-15
per the 2026-05-01 revision):
- D-04, D-05, D-06: per-tick strategy evaluation in kalshi_scan_loop
- D-07, D-08, D-09: sport-family ↔ sport_path mapping + elapsed_minutes helper
- D-10, D-11, D-12: per-strategy dedupe + multi-strategy fire + first-trigger-wins
- D-13, D-14: place_strategy_trade with hardcoded dry_run + balance-driven sizing
- D-23 (supersedes D-15): loop-level trading_paused gate
- D-16, D-17, D-18: combined-filter settlement on both REST + WS paths + P&L math
- D-20 (scanner.py portion): WHAT_IF_STRATEGIES + _evaluate_what_if_strategies + stretch branches + on_lifecycle stretch block — all removed
- D-22: check_stretch_settlements function + call site deleted

Purpose: this single plan satisfies ROADMAP success criteria #2, #3, #4 in full
and #1's "WHAT_IF_STRATEGIES removed" half. Same-file conflicts force serial
tasks within one plan rather than parallel plans.

Output: `src/predictions/scanner.py` is fully Phase-3-compliant. Tests
`tests/test_scanner_strategies.py` (9 tests) and `tests/test_strategy_settlement.py`
(5 tests) drop their xfail markers and pass. `uv run ty check` clean for
scanner.py (api.py + dashboard still red until Plan 03-04). Manual smoke test
boots `pnpm dev:api` and observes one strategy fire write a Trade row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scanner-integration/03-CONTEXT.md
@.planning/phases/03-scanner-integration/03-RESEARCH.md
@.planning/phases/03-scanner-integration/03-PATTERNS.md
@.planning/phases/02-strategy-engine-core/02-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@src/predictions/scanner.py
@src/predictions/db.py
@src/predictions/strategies.py
@src/predictions/espn.py
@src/predictions/kalshi_client.py
@tests/test_scanner_strategies.py
@tests/test_strategy_settlement.py

<interfaces>
Existing scanner.py contracts the executor must compose against (DO NOT re-read
the file again — these are the interfaces). All file:line references match the
post-Plan-03-02 state of master.

From src/predictions/scanner.py:55-90 (existing module-level state — extend with new constants):
```
log = logging.getLogger(__name__)

market_prices: dict[str, dict] = {}
MIN_VOLUME = 50

MIN_SCORE_LEAD = {
    "basketball/nba": 8,
    "basketball/mens-college-basketball": 8,
    "hockey/nhl": 2,
    "football/nfl": 10,
    "football/college-football": 10,
    "baseball/mlb": 3,
    "soccer/eng.1": 2,
    "soccer/esp.1": 2,
    "soccer/usa.1": 2,
    "mma/ufc": 0,
}

SPORTS_GAME_SERIES = [...]
```

From src/predictions/scanner.py:127-200 (existing place_bet — sibling to copy from, NOT modify):
```
async def place_bet(client, opp, max_cost_cents, dry_run):
    yes_price = opp["yes_ask"]
    count = max_cost_cents // yes_price
    if count < 1:
        return None
    total_cost = count * yes_price
    total_profit = count * (100 - yes_price)
    log.info(f"  Order: BUY {count}x YES @ {yes_price}c = ${total_cost / 100:.2f} cost, ...")
    session = get_session()
    trade = Trade(
        ticker=opp["ticker"], event_ticker=opp["event_ticker"], title=opp["title"],
        side="yes", action="buy", count=count, yes_price=yes_price,
        cost_cents=total_cost, potential_profit_cents=total_profit,
        dry_run=dry_run, espn_clock_seconds=opp.get("espn_clock_seconds"),
    )
    if dry_run:
        trade.status = "dry_run"
        session.add(trade); session.commit(); session.close()
        return {"dry_run": True, "count": count, "yes_price": yes_price}
    # ... real-order branch follows; UNTOUCHED in Phase 3
```

From src/predictions/scanner.py:203-239 (existing check_settlements — replace filter):
```
async def check_settlements(client: KalshiClient):
    session = get_session()
    open_trades = (
        session.query(Trade)
        .filter(Trade.status.in_(("placed", "filled")), Trade.dry_run == False)
        .all()
    )
    for trade in open_trades:
        try:
            market = await client.get_market(trade.ticker)
            status = market.get("status", "")
            result = market.get("result", "")
            if status in ("finalized", "settled"):
                if result == trade.side:
                    trade.status = "settled_win"
                    trade.pnl_cents = trade.potential_profit_cents - (trade.fee_cents or 0)
                else:
                    trade.status = "settled_loss"
                    trade.pnl_cents = -trade.cost_cents - (trade.fee_cents or 0)
        except Exception as e:
            log.warning(f"  Failed to check {trade.ticker}: {e}")
    session.commit(); session.close()
```

From src/predictions/scanner.py:828-880 (existing on_lifecycle — closure inside run_scanner; EXTRACT to module level):
```
async def on_lifecycle(msg: dict):
    data = msg.get("msg", {})
    ticker = data.get("market_ticker", "")
    new_status = data.get("market_status", "")
    result = data.get("result", "")
    if new_status in ("finalized", "settled") and ticker:
        log.info(f"WS lifecycle: {ticker} -> {new_status} result={result}")
        session = get_session()
        open_trades = (
            session.query(Trade)
            .filter(
                Trade.ticker == ticker,
                Trade.status.in_(("placed", "filled")),
                Trade.dry_run == False,
            )
            .all()
        )
        for trade in open_trades:
            fee = trade.fee_cents or 0
            if result == trade.side:
                trade.status = "settled_win"; trade.pnl_cents = trade.potential_profit_cents - fee
            else:
                trade.status = "settled_loss"; trade.pnl_cents = -trade.cost_cents - fee

        # Update stretch opportunities — DELETE per D-20
        open_stretches = session.query(StretchOpportunity).filter(...).all()
        # ...

        session.commit(); session.close()
        await record_balance(client)
```

From src/predictions/scanner.py:912-1003 (kalshi_scan_loop — closure that gets the new evaluate_strategies call wired in):
```
async def kalshi_scan_loop():
    nonlocal ticker_sub_sid, lifecycle_sub_sid
    kalshi_interval = 5
    await asyncio.sleep(3)
    while True:
        try:
            cur_price = get_config_int("min_yes_price") or min_yes_price
            cur_bet_percent = float(get_config("bet_percent")) if get_config("bet_percent") else bet_percent
            async with espn_lock:
                current_espn = dict(espn_cache)
                current_espn_fp = dict(espn_final_period_cache)
            # ... discover events, populate market_prices seed ...
            await scan_kalshi_with_espn(client, current_espn, cur_price, cur_bet_percent, dry_run, espn_final_period=current_espn_fp)
            # INSERTION POINT for evaluate_strategies (between scan and check_settlements)
            await check_settlements(client)
            await check_stretch_settlements(client)   # DELETE per D-22
            await record_balance(client)
        except Exception as e:
            log.warning(f"Kalshi scan error: {e}")
        await asyncio.sleep(kalshi_interval)
```

From src/predictions/strategies.py — Phase 2 contract:
```
class Trigger(BaseModel):
    sport: Optional[str] = None
    min_minute: Optional[int] = None
    min_lead: Optional[int] = None
    min_yes_price: Optional[int] = None
    max_yes_price: Optional[int] = None

class Strategy(BaseModel):
    name: str = ""
    description: Optional[str] = None
    triggers: list[Trigger]  # min_length=1

def load_strategies(path: Optional[str] = None) -> list[Strategy]:
    # Returns [] on every error path (missing file, parse error, validation error). Logs at WARNING.
```

From src/predictions/espn.py:16-59 (KALSHI_TO_ESPN authoritative sport_paths + GameState shape):
```
KALSHI_TO_ESPN = {
    "KXNBAGAME": "basketball/nba",
    "KXNHLGAME": "hockey/nhl",
    "KXNFLGAME": "football/nfl",
    "KXMLBGAME": "baseball/mlb",
    "KXNCAAMBGAME": "basketball/mens-college-basketball",
    "KXNCAAFBGAME": "football/college-football",
    "KXEPLGAME": "soccer/eng.1",
    "KXLALIGAGAME": "soccer/esp.1",
    "KXMLSGAME": "soccer/usa.1",
    "KXMLBSTGAME": "baseball/mlb",
}

@dataclass
class GameState:
    espn_id: str; home_team: str; away_team: str
    home_score: int; away_score: int
    period: int; display_clock: str; clock_seconds: float
    state: str  # "in" for live
    status_name: str; sport_path: str
    @property
    def score_diff(self) -> int: return abs(self.home_score - self.away_score)
```

From src/predictions/db.py (post-Plan-03-02):
```
class Trade(Base):
    # ... existing columns
    strategy_name = Column(String, nullable=True, index=True)   # NEW
# StretchOpportunity is DELETED — `from predictions.db import StretchOpportunity` raises ImportError

def get_config(key: str) -> str | None: ...
def get_config_int(key: str) -> int | None: ...
def get_session() -> Session: ...
```

From src/predictions/kalshi_client.py — KalshiClient surface used here:
```
async def get_market(self, ticker: str) -> dict: ...
async def get_balance(self) -> dict: ...
def extract_cents(d: dict, key: str) -> int: ...
def extract_volume(d: dict) -> int: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sport-mapping + elapsed_minutes constants and helpers (D-08, D-09)</name>
  <files>src/predictions/scanner.py</files>
  <read_first>
    - .planning/phases/03-scanner-integration/03-CONTEXT.md D-07 (lines 167–172), D-08 (lines 175–198), D-09 (lines 200–227)
    - .planning/phases/03-scanner-integration/03-RESEARCH.md "Pattern 3: Per-sport elapsed-minutes derivation" (lines 311–387) + "Pattern 4: Module-level frozenset" (lines 389–414) + the verified sport_paths table at line 416–431
    - .planning/phases/03-scanner-integration/03-PATTERNS.md "Sport-family + game-clock constants (D-08, D-09)" placement note
    - src/predictions/espn.py:16-28 (KALSHI_TO_ESPN authoritative source)
    - src/predictions/scanner.py:62-90 (MIN_SCORE_LEAD placement; new constants follow)
  </read_first>
  <action>
Insert NEW module-level constants and ONE helper function in src/predictions/scanner.py, placed immediately AFTER the `MIN_SCORE_LEAD` dict (after line 73) and BEFORE `SPORTS_GAME_SERIES = [...]`. This co-locates the new sport metadata with the existing per-sport catalog.

Insertion content (use these exact literals; values are pre-verified against KALSHI_TO_ESPN):

```
# Sport-family literals (UK terminology, per Phase 2 D-02 OVERRIDE in
# 02-CONTEXT.md lines 482-496 + Phase 3 D-07): trigger.sport in YAML is
# matched to Kalshi/ESPN sport_paths through this map. Only paths actually
# present in espn.KALSHI_TO_ESPN are enumerated; expand when KALSHI_TO_ESPN
# grows.
SPORT_FAMILY_TO_PATHS: dict[str, frozenset[str]] = {
    "football": frozenset({"soccer/eng.1", "soccer/esp.1", "soccer/usa.1"}),
    "basketball": frozenset({"basketball/nba", "basketball/mens-college-basketball"}),
    "baseball": frozenset({"baseball/mlb"}),
    "american_football": frozenset({"football/nfl", "football/college-football"}),
    "hockey": frozenset({"hockey/nhl"}),
    "tennis": frozenset(),  # KXTENNISGAME exists but no ESPN match today
}

# Reverse lookup (path -> family) computed once at module import.
SPORT_PATH_TO_FAMILY: dict[str, str] = {
    path: family
    for family, paths in SPORT_FAMILY_TO_PATHS.items()
    for path in paths
}

# Per-sport regulation period length in seconds. Phase 3 D-09 uses these
# to derive elapsed_minutes from ESPN clock_seconds + period.
SPORT_PERIOD_LENGTH_SECS: dict[str, int] = {
    "basketball/nba": 12 * 60,
    "basketball/mens-college-basketball": 20 * 60,
    "football/nfl": 15 * 60,
    "football/college-football": 15 * 60,
    "hockey/nhl": 20 * 60,
    "soccer/eng.1": 45 * 60,
    "soccer/esp.1": 45 * 60,
    "soccer/usa.1": 45 * 60,
    "mma/ufc": 5 * 60,
}

# Sports with no game clock — min_minute triggers cannot be evaluated;
# elapsed_minutes returns None and the caller skips the trigger.
CLOCKLESS_SPORT_PATHS: frozenset[str] = frozenset({
    "baseball/mlb",
})

# Sports where ESPN's displayClock counts UP cumulatively across the match
# (45:00 at half, 90:00 at full time). Verified by espn.py:88's
# `clock_seconds >= 4500` threshold check (only meaningful on a cumulative
# clock). RESEARCH A1: empirical re-verification recommended before lock.
COUNT_UP_SPORT_PATHS: frozenset[str] = frozenset({
    "soccer/eng.1",
    "soccer/esp.1",
    "soccer/usa.1",
})


def elapsed_minutes(
    sport_path: str, clock_seconds: float, period: int
) -> int | None:
    """Return game-clock minutes elapsed since start, or None if not derivable.

    Returns None for clockless sports (baseball, tennis) — caller logs +
    skips min_minute triggers for these. Returns None for unknown
    sport_paths — caller logs + skips.

    D-09 semantics:
    - Soccer (count-up): clock_seconds is cumulative across the match.
      elapsed = clock // 60.
    - Count-down sports (basketball, football, hockey, mma): each period
      counts down from period_secs to 0. elapsed = (period - 1) *
      period_secs + (period_secs - clock_seconds), divided by 60.
    """
    if sport_path in CLOCKLESS_SPORT_PATHS:
        return None
    period_secs = SPORT_PERIOD_LENGTH_SECS.get(sport_path)
    if period_secs is None:
        return None
    if sport_path in COUNT_UP_SPORT_PATHS:
        return int(clock_seconds // 60)
    completed_periods = max(0, period - 1)
    elapsed_in_current = max(0, period_secs - int(clock_seconds))
    return (completed_periods * period_secs + elapsed_in_current) // 60
```

The constants are placed at module scope so import-order tests (`test_sport_path_to_family`, `test_elapsed_minutes_per_sport` from Plan 03-01) can `from predictions.scanner import SPORT_PATH_TO_FAMILY, elapsed_minutes` directly.

Format with oxfmt 4-space; ruff-clean; ty-clean.
  </action>
  <verify>
    <automated>cd /home/pavel/playground/kalshi-trading && uv run ruff check src/predictions/scanner.py && grep -q "SPORT_FAMILY_TO_PATHS" src/predictions/scanner.py && grep -q "SPORT_PATH_TO_FAMILY" src/predictions/scanner.py && grep -q "def elapsed_minutes" src/predictions/scanner.py && uv run python -c "from predictions.scanner import SPORT_PATH_TO_FAMILY, elapsed_minutes; assert SPORT_PATH_TO_FAMILY['soccer/eng.1'] == 'football'; assert elapsed_minutes('basketball/nba', 60, 4) == 47; assert elapsed_minutes('soccer/eng.1', 2700, 1) == 45; assert elapsed_minutes('baseball/mlb', 0, 9) is None"</automated>
  </verify>
  <done>
- All four greps exit 0.
- The python -c invocation prints nothing (all asserts pass).
- `uv run ruff check src/predictions/scanner.py` exits 0.
- `uv run pytest tests/test_scanner_strategies.py::test_sport_path_to_family tests/test_scanner_strategies.py::test_elapsed_minutes_per_sport -x` runs and the executor has DROPPED the xfail markers from these two tests in this same task — both now pass.
  </done>
</task>

<task type="auto">
  <name>Task 2a: Add helpers (trigger_matches + place_strategy_trade) + refactor scan_kalshi_with_espn signature for D-14</name>
  <files>src/predictions/scanner.py</files>
  <read_first>
    - .planning/phases/03-scanner-integration/03-CONTEXT.md D-10 (lines 232–240), D-11 (lines 242–245), D-12 (lines 247–251), D-13 (lines 254–270), D-14 (lines 272–278)
    - .planning/phases/03-scanner-integration/03-RESEARCH.md "Code Examples" Example 2 (lines 685–735 — place_strategy_trade skeleton)
    - .planning/phases/03-scanner-integration/03-PATTERNS.md "place_bet sibling — session lifecycle to mirror in place_strategy_trade"
    - src/predictions/scanner.py:27-43 (existing imports), :260-310 (place_bet sibling), :301-330 (scan_kalshi_with_espn signature + balance fetch — TARGET of D-14 refactor)
  </read_first>
  <action>
THREE edits: (1) imports, (2) helpers (trigger_matches + place_strategy_trade), (3) D-14 signature refactor on scan_kalshi_with_espn.

Edit 1 — Update imports near scanner.py:27-43.

- Add `from predictions.strategies import Strategy, Trigger, load_strategies` (Strategy and Trigger are used as direct type annotations in trigger_matches and evaluate_strategies — no forward-reference strings).
- Add `from sqlalchemy import or_, and_` (used by Task 3 settlement filter; placing it here keeps Task 2a/Task 3 in the same plan and avoids a separate import edit).

Edit 2 — Insert TWO new module-level functions in scanner.py, placed after `record_balance` (around line 257) and BEFORE the `WHAT_IF_STRATEGIES` block (which Task 3 deletes). The first is the trigger-match predicate; the second is the dry-run trade writer.

```
def trigger_matches(
    trigger: Trigger,
    *,
    family: str | None,
    elapsed: int | None,
    score_diff: int,
    yes_ask: int,
) -> bool:
    """Evaluate a single trigger's AND-conditions.

    Missing field on the trigger means "no constraint on that dimension"
    (per Phase 2 D-03 / STR-02). For min_minute on a clockless sport
    (elapsed is None), the trigger does NOT match — caller logs+skips.
    """
    if trigger.sport is not None and trigger.sport != family:
        return False
    if trigger.min_minute is not None:
        if elapsed is None:
            return False
        if elapsed < trigger.min_minute:
            return False
    if trigger.min_lead is not None and score_diff < trigger.min_lead:
        return False
    if trigger.min_yes_price is not None and yes_ask < trigger.min_yes_price:
        return False
    if trigger.max_yes_price is not None and yes_ask > trigger.max_yes_price:
        return False
    return True


def place_strategy_trade(
    session,
    opp: dict,
    strategy_name: str,
    max_cost_cents: int,
) -> None:
    """Write a dry-run Trade row from a strategy fire (D-13).

    NEVER calls Kalshi REST. Distinct from place_bet's process-level
    DRY_RUN branch — see 03-CONTEXT.md D-13 specifics. yes_price is
    sourced from market_prices cache (Kalshi yes_ask, integer cents).
    """
    yes_price = opp["yes_ask"]
    if not yes_price:
        log.warning("place_strategy_trade: missing yes_ask for %s", opp["ticker"])
        return
    count = max_cost_cents // yes_price
    if count < 1:
        log.info(
            "place_strategy_trade %s: cannot afford any contracts at %dc (budget %dc)",
            strategy_name,
            yes_price,
            max_cost_cents,
        )
        return

    total_cost = count * yes_price
    total_profit = count * (100 - yes_price)
    log.info(
        f"STRATEGY FIRE {strategy_name}: BUY {count}x YES {opp['ticker']} @ {yes_price}c = "
        f"${total_cost / 100:.2f} cost, ${total_profit / 100:.2f} potential profit"
    )

    trade = Trade(
        ticker=opp["ticker"],
        event_ticker=opp.get("event_ticker", ""),
        title=opp.get("title", ""),
        side="yes",
        action="buy",
        count=count,
        yes_price=yes_price,
        cost_cents=total_cost,
        potential_profit_cents=total_profit,
        status="dry_run",
        dry_run=True,
        strategy_name=strategy_name,
        espn_clock_seconds=opp.get("espn_clock_seconds"),
    )
    session.add(trade)
    session.commit()
```

Edit 3 — D-14 SIGNATURE REFACTOR on `scan_kalshi_with_espn`. This is a SHARED-INTERFACE change; the only caller is `kalshi_scan_loop` (verified by grep). Apply BOTH the function definition change AND the in-body lift simultaneously.

Current state (scanner.py:301-326):

```
async def scan_kalshi_with_espn(
    client: KalshiClient,
    espn_final: dict,
    min_yes_price: int,
    bet_percent: float,
    dry_run: bool,
    espn_final_period: dict | None = None,
):
    """Scan Kalshi markets against cached ESPN game state and place bets."""
    opportunities = []
    stretch_opps = []

    if not espn_final and not espn_final_period:
        log.info("No ESPN games in final minutes — skipping Kalshi scan")
        return

    # Fetch available cash for dynamic bet sizing
    try:
        balance_data = await client.get_balance()
        available_cash = balance_data.get("balance", 0)
    except Exception as e:
        log.warning(f"Failed to fetch balance for bet sizing: {e}")
        available_cash = 0

    max_bet_cents = int(available_cash * (bet_percent / 100.0))
```

Refactored target:

```
async def scan_kalshi_with_espn(
    client: KalshiClient,
    espn_final: dict,
    min_yes_price: int,
    max_bet_cents: int,
    dry_run: bool,
    espn_final_period: dict | None = None,
):
    """Scan Kalshi markets against cached ESPN game state and place bets.

    D-14: max_bet_cents is computed ONCE per scan iteration by the
    caller (kalshi_scan_loop) and passed in here. Same value is also
    passed to evaluate_strategies. NO balance fetch inside this function.
    """
    opportunities = []
    stretch_opps = []

    if not espn_final and not espn_final_period:
        log.info("No ESPN games in final minutes — skipping Kalshi scan")
        return

    # max_bet_cents was computed by caller (D-14); no balance fetch here.
```

The body below this point (the for-loop scanning Kalshi markets) is UNCHANGED — it already references `max_bet_cents` later. The deletion is exactly: the `# Fetch available cash for dynamic bet sizing` comment, the try/except `client.get_balance()` block, the `available_cash = ...` assignments, and the `max_bet_cents = int(available_cash * (bet_percent / 100.0))` computation. The `bet_percent` parameter is REMOVED from the signature; verify with grep that `bet_percent` is not referenced anywhere else inside `scan_kalshi_with_espn` (it wasn't — it was only used to derive max_bet_cents).

Format with oxfmt 4-space; ruff-clean; ty-clean.
  </action>
  <verify>
    <automated>uv run ruff check src/predictions/scanner.py && grep -q "def trigger_matches" src/predictions/scanner.py && grep -q "def place_strategy_trade" src/predictions/scanner.py && grep -q "from predictions.strategies import Strategy, Trigger, load_strategies" src/predictions/scanner.py && grep -q "from sqlalchemy import or_, and_" src/predictions/scanner.py && grep -q "max_bet_cents: int," src/predictions/scanner.py && (! grep -nE "bet_percent: float" src/predictions/scanner.py)</automated>
  </verify>
  <done>
- All positive greps exit 0 (helpers added, imports added, scan_kalshi_with_espn now takes max_bet_cents).
- The negative grep `bet_percent: float` returns no matches inside scanner.py (the parameter is gone from scan_kalshi_with_espn; cur_bet_percent local in kalshi_scan_loop remains a `float` cast inline, not a parameter annotation).
- `uv run ruff check src/predictions/scanner.py` exits 0.
- File parseable: `uv run python -c "import predictions.scanner; print(hasattr(predictions.scanner, 'place_strategy_trade'), hasattr(predictions.scanner, 'trigger_matches'))"` prints `True True`.
- NOTE: After this task, scan_kalshi_with_espn's CALL SITE in kalshi_scan_loop is temporarily wrong (still passes cur_bet_percent). Task 2b updates the call site. Until 2b ships, the scanner runtime is broken — tests + ruff still pass since no test directly invokes kalshi_scan_loop.
  </done>
</task>

<task type="auto">
  <name>Task 2b: Add evaluate_strategies + wire kalshi_scan_loop with single max_bet_cents (D-04, D-14)</name>
  <files>src/predictions/scanner.py</files>
  <read_first>
    - .planning/phases/03-scanner-integration/03-CONTEXT.md D-04 (lines 124–132), D-05 (lines 134–141), D-06 (lines 143–164), D-14 (lines 272–278), D-23 (lines 408–417)
    - .planning/phases/03-scanner-integration/03-RESEARCH.md "Code Examples" Example 1 (lines 559–681 — full evaluate_strategies + trigger_matches skeleton)
    - .planning/phases/03-scanner-integration/03-RESEARCH.md Pitfall #2 (D-23 confirms loop-level paused gate) + Pitfall #5 (current_open_markets construction) + Pitfall #6 (first-trigger-wins) + Pitfall #7 (market_prices missing title — extend the seed)
    - .planning/phases/03-scanner-integration/03-PATTERNS.md "Loop-level trading_paused check" + "Open-positions dedupe pre-load — pattern for D-10"
    - src/predictions/scanner.py post-Task-2a state (helpers in place, scan_kalshi_with_espn refactored)
    - src/predictions/scanner.py:912-1003 (kalshi_scan_loop — closure that gets the new evaluate_strategies call wired in)
  </read_first>
  <action>
THREE coordinated edits to complete the per-tick strategy evaluation pipeline.

Edit 1 — Extend the kalshi_scan_loop's market_prices seed at scanner.py:954-957 to include title and event_ticker (Pitfall #7 mitigation). Inside `kalshi_scan_loop` where `if t not in market_prices:` is followed by the seed dict, change the seed to:

```
                                        if t not in market_prices:
                                            market_prices[t] = {
                                                "yes_bid": extract_cents(market, "yes_bid"),
                                                "yes_ask": extract_cents(market, "yes_ask"),
                                                "volume": extract_volume(market),
                                                "title": event.get("title", ""),
                                                "event_ticker": event.get("event_ticker", ""),
                                            }
```

`event.get("title", "")` and `event.get("event_ticker", "")` come from the events response already iterated. ADDITIVE shape change — existing readers use `.get()` semantics so no consumer breaks. Document the additive change in the SUMMARY.

Edit 2 — Insert `evaluate_strategies` as a module-level async function. Place it immediately after `place_strategy_trade` (added in Task 2a):

```
async def evaluate_strategies(
    session,
    espn_final_period: dict,
    max_bet_cents: int,
    strategies: list[Strategy] | None = None,
) -> int:
    """Per-iteration: evaluate every loaded strategy against every live market.

    D-14: max_bet_cents is supplied by the caller (computed ONCE per
    iteration in kalshi_scan_loop) — never recomputed here. D-23
    (supersedes D-15): loop-level trading_paused early-exit. D-04
    cadence: same scan tick (~5s), no new asyncio loop. D-05: load
    strategies once per tick if not provided, log+skip on parse error.
    D-06: markets × strategies × triggers with first-trigger-wins.
    D-10: per-strategy dedupe. D-11: multiple strategies CAN fire on
    the same ticker.

    Returns count of new strategy fires this tick (for logging).
    """
    if get_config("trading_paused") == "true":
        log.info("evaluate_strategies: trading paused via config — skipping tick")
        return 0

    if strategies is None:
        try:
            strategies = load_strategies()
        except Exception as e:
            log.warning("evaluate_strategies: failed to load strategies: %s", e)
            return 0
    if not strategies:
        return 0

    # Pre-load existing (strategy_name, ticker) tuples for dedupe (D-10).
    existing: set[tuple[str, str]] = {
        (sn, t)
        for sn, t in session.query(Trade.strategy_name, Trade.ticker)
        .filter(Trade.strategy_name.isnot(None))
        .all()
    }

    fired_this_tick = 0
    for _series_ticker, espn_games in espn_final_period.items():
        for game in espn_games:
            sport_path = game.sport_path
            family = SPORT_PATH_TO_FAMILY.get(sport_path)
            elapsed = elapsed_minutes(sport_path, game.clock_seconds, game.period)

            for ticker, prices in list(market_prices.items()):
                yes_ask = prices.get("yes_ask")
                volume = prices.get("volume", 0) or 0
                if not yes_ask or volume < MIN_VOLUME:
                    continue
                title = prices.get("title", "")
                if not match_kalshi_to_espn(ticker, title, [game]):
                    continue

                for strategy in strategies:
                    if (strategy.name, ticker) in existing:
                        continue
                    for trigger in strategy.triggers:
                        if not trigger_matches(
                            trigger,
                            family=family,
                            elapsed=elapsed,
                            score_diff=game.score_diff,
                            yes_ask=yes_ask,
                        ):
                            continue
                        try:
                            place_strategy_trade(
                                session=session,
                                opp={
                                    "ticker": ticker,
                                    "event_ticker": prices.get("event_ticker", ""),
                                    "title": title,
                                    "yes_ask": yes_ask,
                                    "espn_clock_seconds": int(game.clock_seconds),
                                },
                                strategy_name=strategy.name,
                                max_cost_cents=max_bet_cents,
                            )
                            existing.add((strategy.name, ticker))
                            fired_this_tick += 1
                        except Exception as e:
                            log.warning(
                                "place_strategy_trade failed strategy=%s ticker=%s: %s",
                                strategy.name,
                                ticker,
                                e,
                            )
                        break  # D-12: first-trigger-wins per (strategy, market) tick

    if fired_this_tick:
        log.info("evaluate_strategies: fired %d strategy trades", fired_this_tick)
    return fired_this_tick
```

`match_kalshi_to_espn` is already imported at scanner.py:38-42; reuse without re-import.

Edit 3 — Wire `evaluate_strategies` into kalshi_scan_loop AND apply the D-14 single-compute. Find the call sequence around scanner.py:986-999:

```
                await scan_kalshi_with_espn(
                    client,
                    current_espn,
                    cur_price,
                    cur_bet_percent,
                    dry_run,
                    espn_final_period=current_espn_fp,
                )

                # Settlement checks as fallback (WS lifecycle handles most)
                await check_settlements(client)
                await check_stretch_settlements(client)
                await record_balance(client)
```

Replace with the D-14 LITERAL pattern: compute max_bet_cents ONCE, pass it to BOTH callees. The eval session is opened OUTSIDE the inner try to avoid UnboundLocalError in finally:

```
                # D-14: compute max_bet_cents ONCE per scan iteration.
                # Reused for both place_bet (via scan_kalshi_with_espn)
                # and evaluate_strategies. NO second balance fetch.
                try:
                    balance_data = await client.get_balance()
                    available_cash = balance_data.get("balance", 0)
                except Exception as e:
                    log.warning(f"Failed to fetch balance for bet sizing: {e}")
                    available_cash = 0
                max_bet_cents = int(available_cash * (cur_bet_percent / 100.0))

                await scan_kalshi_with_espn(
                    client,
                    current_espn,
                    cur_price,
                    max_bet_cents,
                    dry_run,
                    espn_final_period=current_espn_fp,
                )

                # Phase 3 D-04: per-tick strategy evaluation. Reuses
                # max_bet_cents from above (D-14 — same value the
                # live-trade path just used).
                eval_session = get_session()
                try:
                    await evaluate_strategies(
                        eval_session, current_espn_fp, max_bet_cents
                    )
                finally:
                    eval_session.close()

                # Settlement checks as fallback (WS lifecycle handles most)
                await check_settlements(client)
                await record_balance(client)
```

Note the assignment shape: `eval_session = get_session()` is OUTSIDE the inner try, so if a later `await evaluate_strategies(...)` raises, `finally` can call `eval_session.close()` without `UnboundLocalError`. If `get_session()` itself raises, the exception propagates out of the loop's outer try/except as before — no session leak because no session was created.

The `check_stretch_settlements(client)` call site is REMOVED (D-22). The function definition itself is removed in Task 3.

Format with oxfmt 4-space; ruff-clean; ty-clean.
  </action>
  <verify>
    <automated>cd /home/pavel/playground/kalshi-trading && uv run ruff check src/predictions/scanner.py && grep -q "async def evaluate_strategies" src/predictions/scanner.py && grep -q "await evaluate_strategies" src/predictions/scanner.py && grep -q "max_bet_cents = int(available_cash" src/predictions/scanner.py && (! grep -nE "balance_data = await client.get_balance\(\)" src/predictions/scanner.py | grep -v "kalshi_scan_loop\|^\s*[0-9]*:$" | wc -l | grep -q "^[01]$")</automated>
  </verify>
  <done>
- `async def evaluate_strategies` defined; `await evaluate_strategies` called from kalshi_scan_loop.
- `max_bet_cents` is computed exactly ONCE per loop iteration in kalshi_scan_loop and passed to BOTH scan_kalshi_with_espn (positionally) AND evaluate_strategies (positionally).
- Exactly one `client.get_balance()` call inside kalshi_scan_loop's body (verify by grep — only occurrence inside the loop should be the new D-14 fetch; record_balance has its own get_balance call elsewhere, that's expected).
- `! grep -q "await check_stretch_settlements" src/predictions/scanner.py` exits 0 (call site removed; function still exists, deleted in Task 3).
- `uv run ruff check src/predictions/scanner.py` exits 0.
- `uv run ruff format --check src/predictions/scanner.py` exits 0.
- File parseable: `uv run python -c "import predictions.scanner; print(hasattr(predictions.scanner, 'evaluate_strategies'))"` prints `True`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply settlement filters (D-16, D-17), extract on_lifecycle, delete WHAT_IF + check_stretch_settlements (D-20, D-22)</name>
  <files>src/predictions/scanner.py</files>
  <read_first>
    - .planning/phases/03-scanner-integration/03-CONTEXT.md D-16 (lines 295–311), D-17 (lines 313–319), D-18 (lines 321–326), D-20 (lines 356–372), D-22 (lines 401–406)
    - .planning/phases/03-scanner-integration/03-RESEARCH.md "Pattern 2: Combined filter via or_ / and_" + Example 3 (check_settlements) + Example 4 (on_lifecycle)
    - .planning/phases/03-scanner-integration/03-RESEARCH.md Pitfall #1 (E712 — pyproject.toml:32 ignores it; use `==` form)
    - .planning/phases/03-scanner-integration/03-PATTERNS.md "Removals per D-20, D-22" deletion table (lines 270-281) — exact line ranges
    - src/predictions/scanner.py:203-239 (check_settlements), :260-310 (WHAT_IF_STRATEGIES dict), :310-577 (scan_kalshi_with_espn including stretch branches), :580-707 (_evaluate_what_if_strategies), :710-747 (check_stretch_settlements), :828-880 (on_lifecycle closure including stretch update block)
  </read_first>
  <action>
FOUR coordinated edits to complete Phase 3 scanner work.

Edit 1 — Update check_settlements (D-16) at scanner.py:203-239.

Replace the filter:

```
    open_trades = (
        session.query(Trade)
        .filter(Trade.status.in_(("placed", "filled")), Trade.dry_run == False)
        .all()
    )
```

with the combined filter:

```
    open_trades = (
        session.query(Trade)
        .filter(
            Trade.status.in_(("placed", "filled", "dry_run")),
            or_(
                Trade.dry_run == False,
                and_(Trade.dry_run == True, Trade.strategy_name.isnot(None)),
            ),
        )
        .all()
    )
```

The win/loss math block following the filter is REUSED unchanged — `fee = trade.fee_cents or 0` returns 0 for strategy dry-runs (no real order), giving D-18 P&L automatically. Add `"STRATEGY"` vs `"REAL"` log-tag refinement:

```
            if status in ("finalized", "settled"):
                fee = trade.fee_cents or 0
                tag = "STRATEGY" if trade.strategy_name else "REAL"
                if result == trade.side:
                    trade.status = "settled_win"
                    trade.pnl_cents = trade.potential_profit_cents - fee
                    log.info(
                        f"  {tag} WIN: {trade.ticker} settled {result} | "
                        f"P&L: +${trade.pnl_cents / 100:.2f}"
                    )
                else:
                    trade.status = "settled_loss"
                    trade.pnl_cents = -trade.cost_cents - fee
                    log.info(
                        f"  {tag} LOSS: {trade.ticker} settled {result} | "
                        f"P&L: ${trade.pnl_cents / 100:.2f}"
                    )
```

E712 is globally ignored in pyproject.toml:32 — use `Trade.dry_run == False` and `== True` directly; do NOT switch to `is_(False)` (RESEARCH Pitfall #1: emits non-portable `IS FALSE` SQL on SQLite).

Edit 2 — Extract on_lifecycle to module level + apply D-17 filter. The current closure inside `run_scanner` (scanner.py:828-880) is testability-blocked; per PATTERNS.md note, extract it. The closure currently captures `client` for the `await record_balance(client)` tail call — pass `client` as a parameter when extracting.

Define a NEW module-level function (place near check_settlements; uses the same Trade model + or_/and_ filter):

```
async def on_lifecycle_settle(client: KalshiClient, msg: dict) -> None:
    """WS market_lifecycle_v2 handler — settle trades on finalized markets.

    Module-level (extracted from run_scanner closure) so tests can call
    it directly. D-17: combined filter mirrors check_settlements (D-16)
    for symmetry.
    """
    data = msg.get("msg", {})
    ticker = data.get("market_ticker", "")
    new_status = data.get("market_status", "")
    result = data.get("result", "")
    if new_status not in ("finalized", "settled") or not ticker:
        return

    log.info(f"WS lifecycle: {ticker} -> {new_status} result={result}")
    session = get_session()
    open_trades = (
        session.query(Trade)
        .filter(
            Trade.ticker == ticker,
            Trade.status.in_(("placed", "filled", "dry_run")),
            or_(
                Trade.dry_run == False,
                and_(Trade.dry_run == True, Trade.strategy_name.isnot(None)),
            ),
        )
        .all()
    )
    for trade in open_trades:
        fee = trade.fee_cents or 0
        tag = "STRATEGY" if trade.strategy_name else "REAL"
        if result == trade.side:
            trade.status = "settled_win"
            trade.pnl_cents = trade.potential_profit_cents - fee
            log.info(f"  {tag} WIN: {trade.ticker} | P&L: +${trade.pnl_cents / 100:.2f}")
        else:
            trade.status = "settled_loss"
            trade.pnl_cents = -trade.cost_cents - fee
            log.info(f"  {tag} LOSS: {trade.ticker} | P&L: ${trade.pnl_cents / 100:.2f}")
    # Stretch opportunities update block: DELETED per D-20.
    session.commit()
    session.close()
    await record_balance(client)
```

Then DELETE the in-closure `on_lifecycle` function inside `run_scanner` (scanner.py:828-880) and replace the `ws.on("market_lifecycle_v2", on_lifecycle)` registration with a one-line lambda that adapts the new signature:

```
    ws.on("market_lifecycle_v2", lambda msg: on_lifecycle_settle(client, msg))
```

Note: ws.on accepts an awaitable returned by the callback (existing pattern with the original async closure); a lambda returning a coroutine works because `on_lifecycle_settle(client, msg)` is itself a coroutine when awaited by the WS dispatcher. Verify the dispatcher pattern by reading `KalshiWebSocket.on` if uncertain — if the dispatcher requires a callable that returns a coroutine, the lambda form is correct; if it requires an `async def` callback, replace with a thin async wrapper:

```
    async def _lifecycle_cb(msg):
        await on_lifecycle_settle(client, msg)
    ws.on("market_lifecycle_v2", _lifecycle_cb)
```

Pick whichever shape matches the existing dispatcher. Document the choice in the SUMMARY.

Edit 3 — Delete WHAT_IF system (D-20, D-22). Use SYMBOLIC targets (line numbers shift after Task 2a/2b edits — verify each deletion site by grep, not by absolute line number):

- The `StretchOpportunity` symbol in the `from predictions.db import (...)` block — remove (the import is now an ImportError after Plan 03-02).
- The `WHAT_IF_STRATEGIES = { ... }` dict literal — delete the entire dict.
- The `stretch_opps = []` accumulator inside `scan_kalshi_with_espn` AND ALL `meets_stretch_lead` / `stretch_lead` / `reason` / `stretch_opps.append(...)` branches inside the per-market evaluation loop — delete. Keep the full-opportunity branch (the `if meets_price and meets_lead:` block that appends to `opportunities`). The post-deletion shape: each market is either a full opportunity or skipped. If a market does NOT meet price AND lead, `continue` to the next market.
- The `if stretch_opps:` flush block + the `existing_stretch` query + the `StretchOpportunity(...)` creation loop inside `scan_kalshi_with_espn` — delete.
- The `# --- What-If strategy evaluation ---` comment + the `if espn_final_period: _evaluate_what_if_strategies(...)` call site inside `scan_kalshi_with_espn` — delete.
- The `_evaluate_what_if_strategies` function body — delete the entire function definition.
- The `check_stretch_settlements` function body — delete the entire function definition (D-22).
- The `await check_stretch_settlements(client)` call site inside `kalshi_scan_loop` — already removed in Task 2b Edit 3; verify zero remaining occurrences by grep.

After all deletions, scan_kalshi_with_espn's signature `espn_final_period: dict | None = None` remains valid but is no longer USED inside scan_kalshi_with_espn (only inside kalshi_scan_loop where it feeds evaluate_strategies). Decide: keep the parameter for backward compatibility with the call site (kalshi_scan_loop:993 still passes `espn_final_period=current_espn_fp` to scan_kalshi_with_espn), OR drop the parameter and the call-site kwarg simultaneously. Recommended: DROP both since espn_final_period is no longer needed by scan_kalshi_with_espn after the WHAT_IF removal. Verify by grep that `espn_final_period` references inside scan_kalshi_with_espn are zero after deletions.

Also delete the `from predictions.espn import game_meets_timing` import if it becomes orphaned after removing `_evaluate_what_if_strategies` (which was the only consumer). Grep confirms.

Edit 4 — Drop xfail markers in tests/test_scanner_strategies.py (9 tests) and tests/test_strategy_settlement.py (5 tests). All 14 tests should now pass against the post-Task-3 scanner.py. The `test_what_if_strategies_removed` test asserts `WHAT_IF_STRATEGIES` is no longer importable — Edit 3 satisfies this.

Format with oxfmt 4-space; ruff-clean; ty-clean.
  </action>
  <verify>
    <automated>cd /home/pavel/playground/kalshi-trading && uv run ruff check src/predictions/scanner.py && uv run ruff format --check src/predictions/scanner.py && (! grep -q "WHAT_IF_STRATEGIES" src/predictions/scanner.py) && (! grep -q "_evaluate_what_if_strategies" src/predictions/scanner.py) && (! grep -q "StretchOpportunity" src/predictions/scanner.py) && (! grep -q "check_stretch_settlements" src/predictions/scanner.py) && (! grep -q "stretch_opps" src/predictions/scanner.py) && (! grep -q "meets_stretch_lead" src/predictions/scanner.py) && grep -q "or_(\\s*$\\|or_($" src/predictions/scanner.py && grep -q "and_(Trade.dry_run == True, Trade.strategy_name.isnot(None))" src/predictions/scanner.py && grep -q "async def on_lifecycle_settle" src/predictions/scanner.py && uv run pytest tests/test_scanner_strategies.py tests/test_strategy_settlement.py -x</automated>
  </verify>
  <done>
- All negative greps exit 0 (six "must not exist" patterns).
- All positive greps exit 0 (combined-filter expression + on_lifecycle_settle).
- All 14 tests in tests/test_scanner_strategies.py + tests/test_strategy_settlement.py pass (xfail markers removed).
- `uv run ruff check src/predictions/scanner.py` exits 0.
- `uv run ruff format --check src/predictions/scanner.py` exits 0.
- `uv run ty check src/predictions/scanner.py` exits 0 (api.py + dashboard still red until Plan 03-04).
- Manual smoke note (in SUMMARY): boot `pnpm dev:api` with a wide-trigger strategies.yaml entry, observe one strategy fire log line, verify Trade row in DB with strategy_name set + dry_run=True. (Defer to phase-end UAT.)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner async loops ↔ in-memory market_prices cache | WS-populated; readers (scan_kalshi_with_espn + evaluate_strategies) only call `.get()` so missing keys don't crash. |
| scanner ↔ predictions.db Session | One session per scan-loop iteration for evaluation; place_strategy_trade owns its commit; check_settlements + on_lifecycle_settle each open + close their own session. |
| scanner ↔ Kalshi REST | place_bet may call client.create_order. place_strategy_trade does NOT — invariant. on_lifecycle_settle calls record_balance which calls client.get_balance (read-only). |
| scanner ↔ ESPN | Read-only via get_categorized_games (in espn_loop, not modified here). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | strategies.yaml malformed YAML triggers crash | mitigate | load_strategies catches all parse + validation errors and returns []. evaluate_strategies has a defense-in-depth try/except around load_strategies. Both log at WARNING and return — no scan-loop raise. Per D-05. |
| T-3-02 | Tampering / data integrity | Mismatched dry_run flag attribution — a real-money trade row gets dry_run=True+strategy_name, or a strategy fire gets dry_run=False | mitigate | place_strategy_trade HARDCODES dry_run=True + status="dry_run" + strategy_name=name (D-13). place_bet is UNCHANGED (preserves process-level DRY_RUN). Tests test_evaluate_strategies_fires_dry_run_trade and test_strategy_fire_independent_of_dry_run_env assert both branches. Severity: HIGH. |
| T-3-03 | Tampering / data integrity | Settlement filter drift between WS path (on_lifecycle_settle) and REST path (check_settlements) leaving rows un-settled or double-settled | mitigate | D-16/D-17 use the IDENTICAL combined filter literal (or_(dry_run==False, and_(dry_run==True, strategy_name.isnot(None)))). Tests test_settlement_filter_symmetry asserts both paths transition the same row set. Race protection: check_settlements skips already-terminal statuses (`if status not in ("finalized","settled")`); WS-then-REST race produces same result idempotently. Severity: HIGH. |
| T-3-04 | Tampering / kill-switch bypass | trading_paused gate fails — eval runs and writes a row while paused | mitigate | D-23: loop-level early-return at top of evaluate_strategies, mirrors scanner.py:523-525. Test test_trading_paused_blocks_strategy_fire asserts no Trade row when paused="true". Loop-level placement (not callee-level) prevents the "pause between check and write" race per Pitfall #2. Severity: HIGH. |
| T-3-06 | Information disclosure | Log lines containing strategy_name + ticker + yes_price could reveal trading strategy to anyone reading scanner.log | accept | scanner.log is local-only on the Fargate container; the strategy YAML itself is in the repo (already committed). No new exposure surface. |
| T-3-08 | Denial of service | evaluate_strategies iterates markets × strategies × triggers per tick; with N markets * M strategies * K triggers, runtime grows multiplicatively | accept | Production scale: N ≤ 200 active markets, M ≤ 10 strategies, K ≤ 5 triggers each = 10K iterations/tick. Each iteration is pure Python conditionals + one cached dict lookup; sub-millisecond total. The DB pre-load `existing` set is O(1) per check. ESPN match (match_kalshi_to_espn) is the dominant cost — already paid by scan_kalshi_with_espn. |
| T-3-11 | Tampering | Concurrent on_lifecycle_settle on the same ticker + check_settlements double-update | accept | Both paths only transition placed/filled/dry_run → settled_*. Once settled, the status filter excludes the row from the next pass. Idempotent on the same Kalshi result. RESEARCH Security Domain confirms. |
</threat_model>

<verification>
After all tasks complete, run from repo root:

```
uv run ruff check src/predictions/scanner.py
uv run ruff format --check src/predictions/scanner.py
uv run ty check src/predictions/scanner.py

uv run pytest tests/test_scanner_strategies.py tests/test_strategy_settlement.py tests/test_db_migrations.py -x
# Expect: all tests pass; exit 0

uv run pytest tests/
# Full suite: scanner + settlement + migration tests green; api.py-dependent tests
# (test_strategies_api, test_backtest_api, test_sport_stats) may still be red until Plan 03-04 ships D-19+D-21.
# Document red tests in SUMMARY as expected-red until 03-04.

# Manual smoke (deferred to phase UAT):
#   pnpm dev:api   # in one shell
#   tail -f scanner.log    # in another
#   Observe one "STRATEGY FIRE" log line within ~5 minutes
#   sqlite3 predictions.db "SELECT id, ticker, strategy_name, dry_run, yes_price, status FROM trades WHERE strategy_name IS NOT NULL ORDER BY id DESC LIMIT 5;"
```
</verification>

<success_criteria>
- src/predictions/scanner.py contains: SPORT_FAMILY_TO_PATHS, SPORT_PATH_TO_FAMILY, SPORT_PERIOD_LENGTH_SECS, CLOCKLESS_SPORT_PATHS, COUNT_UP_SPORT_PATHS, elapsed_minutes, trigger_matches, place_strategy_trade, evaluate_strategies, on_lifecycle_settle.
- scanner.py imports: `from predictions.strategies import Strategy, Trigger, load_strategies` and `from sqlalchemy import or_, and_`.
- D-14 LITERAL: `kalshi_scan_loop` computes `max_bet_cents` exactly ONCE per iteration via `client.get_balance()` + `cur_bet_percent` multiply, then passes the same value as a positional argument to BOTH `scan_kalshi_with_espn` AND `evaluate_strategies`. `scan_kalshi_with_espn` no longer fetches its own balance.
- `scan_kalshi_with_espn` signature is `(client, espn_final, min_yes_price, max_bet_cents, dry_run, espn_final_period=None)` — `bet_percent` parameter REMOVED, `max_bet_cents` ADDED.
- scanner.py does NOT contain: WHAT_IF_STRATEGIES, _evaluate_what_if_strategies, check_stretch_settlements, StretchOpportunity, stretch_opps, meets_stretch_lead, stretch_lead.
- evaluate_strategies wired into kalshi_scan_loop between scan_kalshi_with_espn and check_settlements.
- check_settlements + on_lifecycle_settle both use the combined or_/and_ filter (D-16, D-17).
- All 9 tests in tests/test_scanner_strategies.py + 5 tests in tests/test_strategy_settlement.py pass (xfail markers removed).
- All 5 tests in tests/test_db_migrations.py still pass.
- uv run ruff check + ruff format --check + ty check on scanner.py exit 0.
- ROADMAP success criteria #2, #3, #4 are demonstrably true (covered by tests above).
- ROADMAP success criterion #1 partial: WHAT_IF_STRATEGIES gone (verified); /api/sport-stats migration is Plan 03-04.
</success_criteria>

<output>
After completion, create `.planning/phases/03-scanner-integration/03-03-SUMMARY.md` documenting:
- All decisions implemented (D-04..D-18, D-20 scanner portion, D-22, D-23) with the in-file location of each
- on_lifecycle extraction recipe — closure → module-level on_lifecycle_settle(client, msg) — and which lambda/wrapper shape matched the KalshiWebSocket.on dispatcher
- The market_prices seed shape change (added title + event_ticker) — flagged as ADDITIVE for any future consumer
- Confirmation that place_bet's dry_run=True branch (process-level DRY_RUN) is UNCHANGED (verify with `grep -A5 'if dry_run:' src/predictions/scanner.py | head -20`)
- The COUNT_UP_SPORT_PATHS soccer cumulative-clock assumption (RESEARCH A1) — empirical re-verification deferred to manual smoke or live UAT
- Notes on api.py + dashboard still being red — Plan 03-04 ships those
- Manual smoke output: one captured "STRATEGY FIRE" log line + the resulting Trade row's columns
</output>

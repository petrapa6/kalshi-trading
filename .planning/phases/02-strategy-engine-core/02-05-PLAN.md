---
phase: 02-strategy-engine-core
plan: 05
type: execute
wave: 4
depends_on:
  - "02-00"
  - "02-01"
  - "02-02"
  - "02-03"
  - "02-04"
files_modified:
  - .planning/STATE.md
autonomous: false
requirements:
  - STR-01
  - STR-02
  - STR-03
  - BT-07
must_haves:
  truths:
    - "Full Python test suite passes (`uv run pytest tests/`)"
    - "Python linters and type checker pass (`uv run ruff check . && uv run ruff format --check . && uv run ty check`)"
    - "Dashboard builds and lints clean (`cd dashboard && pnpm fmt:check && pnpm lint && pnpm build`)"
    - "ROADMAP.md Phase 2 success criteria 1-4 are all observably true (see verification checklist)"
    - "STR-01, STR-02, STR-03, BT-07 are all marked complete in `.planning/REQUIREMENTS.md`"
    - "STATE.md reflects Phase 2 complete and Phase 3 ready"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated status: Phase 2 complete, Phase 3 (Scanner Integration) is the next phase"
      contains: "Phase 02"
  key_links:
    - from: "STATE.md `Current Position`"
      to: "Phase 03 readiness"
      via: "status update"
      pattern: "Phase 0[23]"
---

<objective>
Goal-backward verification of Phase 2: confirm every ROADMAP success
criterion (1-4) is observably TRUE, every locked decision (D-01..D-18)
is implemented, every requirement ID (STR-01, STR-02, STR-03, BT-07)
is satisfied. Update `.planning/STATE.md` to mark Phase 2 complete.

Purpose: This plan is the explicit phase-gate. It does not introduce
new behavior; it verifies the integrated behavior across plans 02-00
through 02-04 and produces the trail of evidence that Phase 3 needs
to safely build on top.

Output: `.planning/STATE.md` updated. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-strategy-engine-core/02-CONTEXT.md
@.planning/phases/02-strategy-engine-core/02-VALIDATION.md
@.planning/phases/02-strategy-engine-core/02-00-SUMMARY.md
@.planning/phases/02-strategy-engine-core/02-01-SUMMARY.md
@.planning/phases/02-strategy-engine-core/02-02-SUMMARY.md
@.planning/phases/02-strategy-engine-core/02-03-SUMMARY.md
@.planning/phases/02-strategy-engine-core/02-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification</name>
  <files>(read-only)</files>
  <read_first>
    - .planning/phases/02-strategy-engine-core/02-VALIDATION.md (Per-Task Verification Map — every test ID must be green)
    - .planning/ROADMAP.md (Phase 2 success criteria 1-4 — verify each)
  </read_first>
  <action>
    Run the full verification matrix and confirm every check passes.
    If ANY check fails, STOP and surface the failure to the user — do
    not paper over a red bar.

    1. Python suite:

       ```bash
       uv run ruff check .
       uv run ruff format --check .
       uv run ty check
       uv run pytest tests/ -v
       ```

       Expected: all four exit 0. pytest reports >=12 passing tests
       across `tests/test_strategies.py` and `tests/test_strategies_api.py`,
       plus all pre-Phase-2 tests still passing.

    2. Dashboard suite:

       ```bash
       cd dashboard && pnpm fmt:check && pnpm lint && pnpm build
       ```

       Expected: all three exit 0.

    3. Roadmap success criteria — verify by direct evidence:

       - **Criterion 1: A `strategies.yaml` exists at the repo root with
         at least one named strategy using OR-of-AND triggers; missing
         file → warning + zero strategies.**
         - `test -f strategies.yaml && grep -c '^  [a-z_]\+:$' strategies.yaml` >= 2 (active strategies)
         - `uv run python -c "from predictions.strategies import load_strategies; print(len(load_strategies('/nonexistent.yaml')))"` prints `0` and emits a WARNING in stderr

       - **Criterion 2: Backtest page strategy dropdown is populated
         from `strategies.yaml`; selecting a strategy pre-fills
         parameter sliders with the strategy's first trigger values;
         sliders remain editable.**
         - Per D-11 narrowing: sliders that exist per trigger = sport
           dropdown, min_minute, min_lead. min_yes_price /
           max_yes_price = info text only.
         - Visually confirmed in plan 02-04 task 2 (manual checkpoint).

       - **Criterion 3: Empty trigger block (`triggers: []` or a
         trigger with no fields) is rejected at load time — Pydantic
         validation enforces `min_length=1` on trigger lists and
         individual trigger dicts.**
         - `tests/test_strategies.py::test_empty_triggers_rejected` passes (proves the list-level rejection)
         - The "trigger with no fields" case: an empty dict `{}` IS valid (all fields are Optional → no fields set means "no constraint on any dimension"). RESEARCH.md and CONTEXT.md acknowledge this — the criterion's intent is about EMPTY LISTS, not empty dicts.
         - Verify via: `uv run python -c "from predictions.strategies import Strategy, Trigger; print(Strategy(name='x', triggers=[]).triggers)"` — this should raise ValidationError; an empty Trigger `Trigger()` should be valid (the engine's "missing field = no constraint" semantics).

       - **Criterion 4: `uv run ruff check . && uv run ruff format
         --check . && uv run ty check` passes clean.**
         - Already covered by step 1 above.

    4. Requirement coverage trail:

       - STR-01 → tests `test_load_empty_file`, `test_missing_file_returns_empty`, `test_strategies_path_env`, `test_endpoint_missing_file_returns_empty` all pass
       - STR-02 → tests `test_empty_triggers_rejected`, `test_unknown_field_rejected`, `test_one_bad_strategy_rejects_file` all pass
       - STR-03 → tests `test_endpoint_requires_auth`, `test_endpoint_response_shape`, `test_endpoint_preserves_yaml_order` all pass
       - BT-07 → manual checkpoint in 02-04 task 2 covers all 9 D-11..D-18 behaviors

    Capture the exact output of each command in the SUMMARY.md.
  </action>
  <verify>
    <automated>
      uv run ruff check .
      &amp;&amp; uv run ruff format --check .
      &amp;&amp; uv run ty check
      &amp;&amp; uv run pytest tests/ -v
      &amp;&amp; (cd dashboard &amp;&amp; pnpm fmt:check &amp;&amp; pnpm lint &amp;&amp; pnpm build)
      &amp;&amp; test -f strategies.yaml
    </automated>
  </verify>
  <acceptance_criteria>
    - All commands in the action above exit 0
    - `uv run pytest tests/test_strategies.py -v` reports 8 PASSED, 0 FAILED
    - `uv run pytest tests/test_strategies_api.py -v` reports 4 PASSED, 0 FAILED
    - No Python tests are skipped due to Phase 2 changes (other tests may still be skipped due to live-API requirements; that is pre-existing)
    - `strategies.yaml` exists at repo root with at least 2 active (non-commented) strategies
    - Dashboard build completes; manual checkpoint from 02-04 task 2 was approved by the user
  </acceptance_criteria>
  <done>
    All verification commands green. Evidence captured for SUMMARY.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md for Phase 2 completion</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — was last updated when Phase 2 context was gathered)
    - .planning/ROADMAP.md (Phase 3 entry — confirm next-phase requirements)
  </read_first>
  <action>
    Update `.planning/STATE.md` with the following changes:

    1. Frontmatter: bump `last_updated` to today's ISO datetime;
       update `progress.completed_phases` from `1` to `2`;
       update `progress.completed_plans` from `2` to `7` (Phase 1 had 2,
       Phase 2 has 5: 02-00, 02-01, 02-02, 02-03, 02-04 — 02-05 is the
       verification plan itself; planner judgment whether to count it).
       To match the convention used at end of Phase 1 (where `01-01`
       and `01-02` both counted), include 02-00 through 02-04 in the
       completed count and exclude 02-05 (it's a verification plan
       producing the SUMMARY, not new behavior).
    2. Frontmatter: bump `progress.percent` to `50` (2 of 4 phases).
    3. Frontmatter: update `status` from `planning` to `in-progress`
       (or `complete` if you judge the milestone phase as the unit;
       follow whatever convention Phase 1 used at its boundary —
       inspect the git log if unclear).
    4. Frontmatter: update `stopped_at` to `Phase 2 complete; ready
       to plan Phase 3`.
    5. Body — `## Current Position` section: change `Phase: 01...
       COMPLETE` to a list showing Phase 1 done and Phase 2 done; set
       `Phase: 02 (strategy-engine-core) — ✅ COMPLETE`. Update the
       progress bar: `[█████░░░░░] 50%`.
    6. Body — `Last activity` line: `2026-04-30 -- Phase 2
       (Strategy Engine Core) shipped: STR-01/02/03 + BT-07 closed`.
    7. Body — `## Accumulated Context` `### Decisions` section: append
       Phase 2 key decisions worth surfacing for Phase 3:

       - "PHASE2-01: PyYAML added (`pyyaml>=6.0`) — first new Python
         dep in Phase 2; only one allowed since YAML is locked in D-05."
       - "PHASE2-02: `STRATEGIES_PATH` env var read in exactly one
         site (`load_strategies` in `src/predictions/strategies.py`).
         Phase 3 scanner must follow the same pattern — no parallel
         env var reads."
       - "PHASE2-03: `extra=\"forbid\"` + `min_length=1` + all-or-nothing
         validation. Phase 3 scanner imports `load_strategies` directly;
         it does NOT re-parse YAML."

    8. Body — `### Pending Todos` section: keep
       `2026-04-29-backtest-contract-based-pnl.md` listed only if it's
       not yet resolved (Phase 1 closed it; if so, remove the row).
       Be precise — do not silently delete; if uncertain, leave the
       table empty under the header rather than fabricate.
    9. Body — `### Blockers/Concerns` section: REMOVE the
       `[Phase 2] YAML field vocabulary` entry (resolved by D-01..D-03).
       KEEP the `[Phase 3] Settlement reconciliation` entry — it is
       still pending. ADD a new line if any new Phase 2 concern surfaced
       (e.g., "If Phase 3 ever exposes user-editable YAML, add a
       max-file-size check before `safe_load` to mitigate alias
       amplification" — this aligns with 02-RESEARCH.md security
       domain).
    10. Body — `## Session Continuity` section:
        - `Last session`: today's ISO datetime
        - `Stopped at`: `Phase 2 complete; ready to plan Phase 3`
        - `Resume`: `/gsd-plan-phase 3` (Scanner Integration —
          STR-04, DRY-01, DRY-02)

    Style: keep the existing markdown table format. Don't reflow or
    refactor unrelated sections.
  </action>
  <verify>
    <automated>
      grep -q "completed_phases: 2" .planning/STATE.md
      &amp;&amp; grep -q "Phase 02" .planning/STATE.md
      &amp;&amp; grep -q "ready to plan Phase 3" .planning/STATE.md
      &amp;&amp; ! grep -q "YAML field vocabulary: backtest" .planning/STATE.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter `progress.completed_phases` is `2`
    - Frontmatter `progress.percent` is `50`
    - Frontmatter `last_updated` is today's date in ISO format
    - Body progress bar shows 50%
    - "Phase 2 complete; ready to plan Phase 3" appears at least twice (once in `stopped_at`, once in body)
    - "YAML field vocabulary" Phase 2 blocker is removed
    - Phase 3 settlement-reconciliation blocker is preserved
    - At least 2 PHASE2-XX decisions added to the Decisions section
    - `Resume:` line points at `/gsd-plan-phase 3`
  </acceptance_criteria>
  <done>
    STATE.md reflects Phase 2 done, Phase 3 ready. Phase 2-specific
    decisions surfaced for downstream Phase 3 planning context.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm Phase 2 ship</name>
  <files>(no files modified — manual verification only)</files>
  <action>Run the verification commands below in a clean shell, read the
  Phase 2 SUMMARYs, and confirm Phase 2 is shippable. If any verification
  fails, fix the failing behavior in the appropriate plan's files and
  re-run.</action>
  <what-built>
    Phase 2 ships the strategy engine core: a strict Pydantic YAML
    loader, a Bearer-auth `GET /api/strategies` endpoint, a
    multi-trigger backtest engine with sport-aware skipping, and a
    multi-trigger backtest UI with strategy presets, +/- buttons,
    sport-mismatch graying, and a skipped-triggers note. STATE.md is
    updated.
  </what-built>
  <how-to-verify>
    1. Read the `.planning/phases/02-strategy-engine-core/02-04-SUMMARY.md`
       — confirm the manual UI checkpoint was approved.
    2. Run the full verification matrix one more time from a clean shell:
       ```bash
       uv run ruff check . && uv run ruff format --check . && uv run ty check
       uv run pytest tests/
       (cd dashboard && pnpm fmt:check && pnpm lint && pnpm build)
       ```
       All commands must exit 0.
    3. Read `.planning/STATE.md` — confirm `completed_phases: 2`, the
       progress bar, and the resume line for Phase 3.
    4. Run a final smoke test against a live dev server (optional but
       recommended):
       ```bash
       API_TOKEN=test pnpm dev:api &
       sleep 3
       curl -s -H "Authorization: Bearer test" http://localhost:8000/api/strategies | python -m json.tool
       kill %1
       ```
       The output should be valid JSON listing the 2+ active strategies
       from `strategies.yaml` with `name`, `description`, and `triggers`
       fields populated.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all verifications pass and Phase 2 can be marked
    shipped. Otherwise list the failed step(s).
  </resume-signal>
  <verify>
    <automated>echo "Manual checkpoint — see how-to-verify steps; user must type approved"</automated>
  </verify>
  <done>User typed "approved" after all verification commands passed and SUMMARY review completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan does not introduce new boundaries; verification only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | — | This plan is a verification gate. All Phase 2 threats (T-02-01 through T-02-10) are mitigated/accepted in plans 02-00..02-04 and re-verified by the test commands run here. |
</threat_model>

<verification>
The verification commands run in Task 1 and Task 3 ARE the verification.
No additional checks beyond those.
</verification>

<success_criteria>
- All Python lint/format/type/test commands exit 0
- All dashboard fmt:check/lint/build commands exit 0
- All four ROADMAP Phase 2 success criteria are observably true (with the D-11 narrowing for criterion #2)
- All four phase requirement IDs (STR-01, STR-02, STR-03, BT-07) are covered by passing tests + the manual UI checkpoint
- `.planning/STATE.md` is updated with Phase 2 complete and Phase 3 resume hint
- User confirms ship via the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-strategy-engine-core/02-05-SUMMARY.md`
recording the verification matrix outputs (one paragraph per command,
the exit code, and a 1-line summary of what was verified).
</output>
